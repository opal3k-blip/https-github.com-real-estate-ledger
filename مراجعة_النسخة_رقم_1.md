# مراجعة النسخة رقم 1 — منظومة مستكشف الفرص العقارية (Opal)
تاريخ المراجعة: 2026-09-14 — بناءً على نسخة المستودع المحلي `reop-github` كما هي على جهازك وقت المراجعة.

هذه مراجعة **مستقلة**: لم أكتفِ بقراءة تقارير التحقق (Validation Reports) المرفقة، بل أعدت تشغيل الاختبارات الفعلية على الكود الحقيقي (محاكي Firestore حقيقي، تنفيذ حقيقي لمحرك الحسابات المالية، فحص صياغي كامل)، وقرأت كوداً لم تغطِه أي من التقارير (خاصة `functions/index.js`) لأتحقق من نقاط لم تُذكر صراحة.

---

## ⚠️ 1. تنبيه أمني عاجل (خارج نطاق الكود نفسه)

ملفا الأعطال (`report.20260914...json`) اللذان وصلا ضمن ملفات المشروع ليسا من ملفات المنظومة — هما تقرير OOM من أداة GitHub Copilot CLI على جهازك، وكانا يحتويان **رمز GitHub OAuth حقيقي مكشوف بالنص الصريح** (تمت إزالته من هذا المستند عمداً — كان يتكرر في حقلي `GH_TOKEN` و`COPILOT_GH_ACCOUNT_...`؛ راجع سجل الأمان لتأكيد أنه أُبطِل بالفعل في ٢٠٢٦-٠٩-١٤). لا تُرفع هذان الملفان لأي مكان — يجب حذفهما نهائياً من مجلد المشروع (تم ذلك بالفعل في هذا الإصلاح).

---

## 2. التحقق المستقل من تقارير الاعتماد (Stages 4–7-a)

لم أُصدِّق التقارير كما هي؛ أعدت تشغيل ما يمكن تشغيله فعلياً في بيئة معزولة:

| الفحص | كيف تم | النتيجة الفعلية |
|---|---|---|
| صحة صياغة JavaScript | `node --check` على كل ملف (58 ملفاً) + `scripts/check-js.mjs` | ✅ نظيفة بالكامل، تطابق ادعاء `docs/SECURITY_RULES_REVIEW.md` |
| محرك الحسابات المالية | تشغيل `run_tests_node.js` فعلياً (يحمّل `core.js` في VM حقيقي وينفّذ 23 اختباراً على `compute()`) | ✅ **23/23 PASS** — نفس ما تدّعيه README، تم التحقق منه بالتنفيذ لا بالقراءة |
| قواعد Firestore | تثبيت `@firebase/rules-unit-testing` + `firebase-tools` فعلياً، وتشغيل محاكي Firestore حقيقي (`firebase emulators:exec`) ضد `firestore.rules` الحالي | ✅ **105 تأكيد (assertion)، صفر فشل** — "ALL PASSED against a real Firestore emulator, not a mock"، خروج بالرمز 0 |
| اختبار الأداء | `scripts/performance-smoke.mjs` | ✅ 10,000 سجل خلال ~2.1 ثانية — لا مشكلة أداء ظاهرة |
| نقاط التوسّع المعمارية (extension points) | فحص وجود كل من `registerTopbarButton/BodyView/DetailSection/ActionHandler/WizardStepExtra/OpportunitySchemaExtender/DataCollection/AssetLinkGuard/BeforeOpportunitySave` في `core.js`، واستخدامها الفعلي في 31 موضعاً عبر ملفات `features/` | ✅ البنية المعمارية الموصوفة في README حقيقية وليست ادعاءً نظرياً |
| عزل وضع الديمو (`?demo=1`) | تتبّع متغيّر `DEMO_MODE` ومتغيّر `DB` في `core.js` | ✅ `DB` يبقى `null` في وضع الديمو — لا اتصال فعلي بـ Firestore الحقيقي، يطابق ادعاء README |

**الخلاصة**: التقارير المرفقة لا تبالغ. هذا نادر نسبياً في مشاريع موثَّقة ذاتياً بهذا الحجم، ويستحق الثقة كأساس — لكن هذا لا يعني عدم وجود فجوات؛ القسم التالي يوثّق ما لم تذكره التقارير.

---

## 3. الأمان والحوكمة

### 3.1 نقطة حرجة غير مذكورة صراحة في تقارير المراحل: Cloud Functions موجودة لكنها **غير مفعَّلة من العميل إطلاقاً**

`functions/index.js` يحتوي ثلاث دوال قوية جداً لم تُذكر في README ولا في أي من تقارير Stage 4-7a (فقط `docs/SECURITY_RULES_REVIEW.md` يلمّح لوجودها في توصية أخيرة مقتضبة):

- `approveOpportunity` — يعيد تنفيذ اعتماد لجنة الاستثمار داخل معاملة خادمية (transaction) حقيقية.
- `linkAssetToFund` — يعيد التحقق من كل شروط ربط الأصل بالصندوق (قرار IC، السقف، السيولة المتاحة) خادمياً.
- `postCapitalCall` — **يتحقق من عدم تجاوز نداء رأس المال لالتزام المستثمر عبر استعلام حقيقي على قاعدة البيانات** (`committedForInvestorTx` / `paidCallsForInvestorTx`).

**المشكلة**: بحثت في `src/` بالكامل — لا يوجد أي استدعاء `httpsCallable` ولا حتى تحميل مكتبة `firebase-functions` في `index.html`. أي أن هذه الدوال **كود ميت اليوم**: مكتوبة، ومختبَرة بنيوياً (اختبار 022 في `validation-suite.cjs` يتحقق فقط من وجود النص في الملف، لا من استدعائها الفعلي)، لكن العميل (الواجهة) لا يستدعيها إطلاقاً. الواجهة ما زالت تكتب مباشرة على Firestore عبر `ic-workflow.js` / `capital-allocation-engine.js` / `core.js`.

**الأثر الفعلي**: تحقّق "نداء رأس المال لا يتجاوز التزام المستثمر" (`validateIfDraft` في `core.js`, السطر ~782) موجود **فقط في جافاسكريبت العميل** — وهو تحقّق قابل للتجاوز (أي شخص يملك صلاحية كتابة على Firestore ويستخدم SDK مباشرة من console المتصفح، بدل واجهة التطبيق، يتخطى هذا الفحص تماماً). `firestore.rules` نفسها **لا** تتحقق من هذا (تتحقق فقط من إشارة المبلغ وتسلسل الحالات pending→approved→paid، لا من مجموع الالتزامات عبر مستندات أخرى — وهذا تحقّق يصعب تنفيذه بكفاءة داخل Security Rules نفسها أصلاً، ولهذا بالضبط صُممت هذه الدوال). خلاصة: الحارس الحقيقي لهذا التحقق تحديداً هو الواجهة فقط اليوم، وليس قاعدة البيانات.

**التوصية**: هذه ليست مشكلة تصميم — التصميم صحيح ومتوقَّع من نموذج "دفتر محاسبي حقيقي". لكنها فجوة تنفيذ فعلية: يجب (أ) نشر هذه الدوال (`firebase deploy --only functions`، تتطلب خطة Blaze)، (ب) تحميل SDK الخاص بـ Functions في `index.html`، (ج) تعديل نقاط الحفظ في `ic-workflow.js`/`capital-allocation-engine.js`/نموذج نداء رأس المال في `core.js` لتستدعي هذه الدوال بدل الكتابة المباشرة. هذا عمل تطوير حقيقي، وليس مجرد ضغطة نشر — أنصح بعدم اعتباره "منجزاً 100%" حتى تُستكمَل هذه الخطوة الثالثة تحديداً.

### 3.2 مقاومة XSS — فحصتها يدوياً لأنها لم تُذكر في أي تقرير

المنظومة مبنية على توليد HTML عبر Template Literals يُحقَن مباشرة بـ`app.innerHTML =` — نمط عالي الخطورة إن لم يُنفَّذ بانضباط. فحصت:
- دالة `esc()` (تُهرِّب `& < > " '`) مستخدَمة **358 مرة** عبر الكود.
- كل حقول الإدخال الحرة (`F.text`, `F.textarea`, `F.select`) تمرّ القيمة عبر `esc()` قبل وضعها في HTML.
- عيّنة من حقول نصية حساسة (اسم الفرصة، ملاحظات العناية الواجبة، وصف المساهمة العينية) — جميعها مُهرَّبة في كل موضع عرض تحقّقت منه.

**لم أجد ثغرة XSS في العيّنة التي فحصتها** — هذا انضباط حقيقي نادر في تطبيقات مبنية بهذا الأسلوب (بلا React/framework يهرّب تلقائياً). لا أستطيع تأكيد 100% من كل الملفات (الحجم كبير جداً لفحص كل سطر)، لكن النمط المتّبع منهجي وليس عشوائياً، وهذا مؤشر جيد.

### 3.3 قواعد Firestore — تأكيد إضافي على ما ذكرته التقارير

بالإضافة للتحقق بالمحاكي أعلاه، راجعت `firestore.rules` سطراً بسطر: منطق `attributionHonest()`، `icOnlyChange()`، `capitalAllocationOnlyChange()`، وبوابة `ledgerStatusTransitionOk()` كلها متسقة منطقياً ولا تحتوي ثغرة تخطٍّ واضحة (لا مسار يسمح بـ`pending→paid` مباشرة، ولا مسار يسمح بتغيير حقل مالي "مموَّهاً" مع تغيير `ic` أو `capitalAllocation`). هذا يطابق نتائج المحاكي الفعلية.

---

## 4. جودة الكود والبنية المعمارية

- **الحجم**: `src/core.js` وحده 618 كيلوبايت (~15,443 سطراً إجمالي `src/` كله بين core+features). هذا ملف ضخم جداً لملف واحد. القرار المعماري الموثَّق في README ("لا يُلمَس core.js إطلاقاً، كل ميزة جديدة في ملف مستقل عبر نقاط توسّع") هو تخفيف ذكي لمخاطر هذا الحجم، وتحقّقتُ أنه مطبَّق فعلاً (31 استخدام حقيقي لـ`registerActionHandler` وحده عبر ملفات `features/`) — لكنه تخفيف لمخاطر إضافة الميزات، وليس حلاً لصعوبة قراءة/صيانة `core.js` نفسه لأي مطوّر جديد يحتاج فهمه.
- **التوثيق داخل الكود**: تعليقات عربية مطوَّلة تشرح *لماذا* لا *ماذا* فقط (مثال: `roles-permissions.js`, `firestore.rules`) — جودة توثيق أعلى من المعتاد في مشاريع بهذا الحجم.
- **اتساق التسمية**: وجدت ملفين بنفس المحتوى تقريباً — `.firebaserc` (المُستخدَم فعلياً من Firebase CLI) و`firebaserc` بدون نقطة (نفس المحتوى، على الأغلب ملف زائد من محاولة سابقة). لا يسبب أي مشكلة وظيفية لكنه تنظيف بسيط مستحق (`.gitignore` الحالي لا يستثنيه، فهو على الأغلب متتبَّع في git فعلاً).
- **الاختبارات**: `run_tests_node.js` و`tests/rules` كلاهما اختبار "صندوق أسود" حقيقي (لا يُحاكي `core.js`، بل يحمّله وينفّذه فعلياً)، وهذا أعلى من متوسط جودة الاختبار في مشاريع مشابهة.

---

## 5. جاهزية النشر الفعلي على Firebase الحي (`real-estate-ledger-f85a6`)

نقاط يجب تأكيدها **قبل** اعتبار هذه النسخة "منشورة وجاهزة" فعلياً — لا يمكنني التحقق منها من هنا لأنها تتطلب الدخول على Firebase Console بحسابك:

1. **حالة نشر `firestore.rules` الفعلية غير مؤكَّدة**: تقرير Stage 6-b يذكر صراحة أن بوابة الاعتماد (`approved` state) **لم تُنشر على المشروع الحي بعد**، وكانت بانتظار موافقتك. الملف المحلي الذي فحصته يحتوي هذه البوابة فعلاً — لكن هذا لا يثبت أنها *منشورة*. تحقّق من [Firebase Console → Firestore → Rules](https://console.firebase.google.com/project/real-estate-ledger-f85a6/firestore/rules) وقارن التاريخ/المحتوى مع نسخة المستودع.
2. **`storage.rules` غير مربوط بـ`firebase.json` إطلاقاً**: فحصت `firebase.json` في جذر المستودع — يحتوي `firestore` و`functions` فقط، **لا يوجد مفتاح `"storage"`** يشير لـ`storage.rules`. هذا يعني أن `firebase deploy` لا ينشر قواعد التخزين هذه أبداً حالياً، رغم أن الملف مكتوب وصحيح. إن كانت صور/ملفات `opportunity-media` مُستخدَمة فعلاً، تحقّق يدوياً من قواعد Storage الفعلية في Console — قد تكون لا تزال على الإعداد الافتراضي.
3. **Cloud Functions تتطلب خطة Blaze** ولم تُنشر بعد حسب آخر تقرير متاح لي (Stage 7-a) — تحقّق من حالتها الحالية في Console → Functions.
4. **معرّف المشروع متّسق** عبر كل الملفات التي فحصتها (`.firebaserc`, `firebase-config.js`, `firebase.json` الفرعي في `tests/rules`) — `real-estate-ledger-f85a6` في كل مكان، لا تعارض هنا.

---

## 6. التوصيات المرتّبة بالأولوية

1. **فوري، خارج الكود**: إبطال رمز GitHub المسرَّب (القسم 1).
2. **قبل الاعتماد على أي اعتماد رأس مال حقيقي**: تأكيد يدوي من Firebase Console أن `firestore.rules` الحية تطابق نسخة المستودع (خصوصاً بوابة `approved`) — لا تفترض أن "مكتوب محلياً" = "منشور فعلياً".
3. **إغلاق فجوة تنفيذ حقيقية**: إما (أ) ربط الواجهة فعلياً بـ`approveOpportunity`/`linkAssetToFund`/`postCapitalCall` بعد نشرها، أو (ب) إن قررتم تأجيل ذلك، توثيق صراحة في README أن هذه الدوال "جاهزة لكن غير مفعَّلة" حتى لا يظن أي مطوّر لاحق أنها تعمل.
4. **ربط `storage.rules` بـ`firebase.json`** (إضافة مفتاح `"storage": {"rules": "storage.rules"}`) قبل أي `firebase deploy` مستقبلي، والتأكد من نشرها.
5. تنظيف: حذف ملف `firebaserc` الزائد (بدون نقطة).

---

## ملحق: ماذا شغّلت فعلياً (للشفافية الكاملة)

- `node --check` على 58 ملف JS (src + functions + tests) — عبر `scripts/check-js.mjs` ويدوياً.
- `node run_tests_node.js` — 23/23 PASS.
- `npm install` حقيقي داخل `tests/rules/` (730 حزمة) ثم `npm test` الذي يشغّل `firebase emulators:exec --only firestore` فعلياً (يتطلب Java، متوفر في بيئتي) — 105 تأكيد، صفر فشل.
- `npm install` داخل `functions/` (242 حزمة) والتحقق من `npm run lint` (`node --check index.js`).
- `node scripts/performance-smoke.mjs` — 10,000 سجل / 2.1 ثانية.
- قراءة كاملة لـ`functions/index.js`، `firestore.rules`، `roles-permissions.js`، `firebase.json`، `SECURITY_RULES_REVIEW.md`.
- بحث نصي (`grep`) عبر كل `src/` عن: `eval(`، `innerHTML =`، أسرار/مفاتيح مكتوبة يدوياً، استخدام `httpsCallable`، تغطية `esc()`.

لم أقرأ كل سطر من كل ملف (`core.js` وحده 618 كيلوبايت) — الفحص ركّز على المسارات الحرجة أمنياً/محاسبياً ونقاط الالتقاء بين الطبقات (عميل ↔ Firestore rules ↔ Cloud Functions)، وهذا عادة حيث تختبئ الفجوات الحقيقية في مشاريع من هذا النوع.
