# مهام قادمة — إعادة تصميم التقارير (Print / Excel / PowerPoint) + خارطة الأنظمة الموسّعة

هذا الملف يوثّق ملاحظات وتوجيهات وردت من المستخدم (٨ سبتمبر ٢٠٢٦) حول تطوير التقارير الثلاثة
الحالية (الطباعة، Excel، PowerPoint) إلى مستوى مؤسسي كامل، بالإضافة إلى إعادة صياغة خارطة
تطوير النظام كـ"عشرة محركات" (Engines). لم تُنفَّذ هذه البنود بعد — هي **مهام مُسجَّلة للتنفيذ
لاحقاً** (على الأرجح ضمن المرحلة ٢/٣ من خطة التطوير الحالية، بعد إكمال أنظمة المرحلة ١
الأساسية: Pipeline ✅، Audit Trail ✅، DD/Risk/Score/IC Workflow قيد البناء).

---

## 1) الطباعة (Print) — من Investment Memo إلى Investment Committee Book

**الوضع الحالي:** الطباعة أقرب إلى مذكرة استثمارية جيدة (Investment Memo)، وليست بعد كتاباً
احترافياً كاملاً بمستوى لجنة استثمار (IC Book).

**التسلسل المطلوب للمذكرة المطبوعة (بهذا الترتيب بالضبط):**

1. Cover Page
2. Executive Investment Recommendation
3. Opportunity Snapshot
4. Investment Thesis
5. Site / Location
6. Acquisition & Land Economics
7. Development / Operating Plan
8. Sources & Uses
9. Financial Returns
10. Cash Flow
11. Debt & Financing
12. Sensitivity
13. Scenario Analysis
14. Market Comparables
15. Risks & Mitigations
16. Due Diligence Status
17. IC Checklist
18. Negotiation Strategy
19. Conditions Precedent
20. Final Recommendation
21. Sources / Evidence / Disclaimer

**الهدف:** تقرير يمكن إرساله فعلياً للجنة الاستثمار، لا مجرد Print View من شاشة التفاصيل.
ملاحظة: عدة أقسام (Sensitivity، Scenario Analysis، Market Comparables، Risks، DD Status،
Negotiation Strategy) تعتمد على أنظمة لم تُبنَ بعد (Phase 2 من خطة الأنظمة) — فإعادة تصميم
الطباعة يجب أن تأتي بعد أو بالتوازي مع بناء تلك الأنظمة، لا قبلها.

---

## 2) Excel — من Export إلى Investment Underwriting Workbook

**تقييم المستخدم للوضع الحالي:** أفضل مما يبدو من الواجهة — مبني فعلياً بـExcelJS لا كجدول خام:
تنسيق Headers، Freeze Panes، Print Area، تكرار Header عند الطباعة، Zebra rows، Number formats،
Borders، أعمدة بعروض محددة، معادلات Excel حقيقية (IRR/XIRR)، أكثر من Sheet (بما فيها Investor/Fund
Ledger منفصل). هذا موثّق في الكود كما هو.

**الفرصة:** تحويله من "تصدير" إلى **Investment Underwriting Workbook** — نسخة تحليلية قابلة للتدقيق
بالكامل. الهيكل المقترح (٢١ ورقة):

| Sheet | الوظيفة |
|---|---|
| 00_IC Dashboard | لوحة القرار |
| 01_Opportunity | بيانات الفرصة |
| 02_Assumptions | جميع الافتراضات |
| 03_Sources & Uses | مصادر واستخدامات الأموال |
| 04_Development | برنامج التطوير |
| 05_Revenue | الإيرادات |
| 06_OPEX | المصروفات |
| 07_Debt | التمويل والديون |
| 08_Project CF | Cash Flow |
| 09_Equity CF | Equity Cash Flow |
| 10_Returns | IRR/MOIC/NPV/CoC |
| 11_Sensitivity | Sensitivity Tables |
| 12_Scenarios | Base/Upside/Downside/Stress |
| 13_Comparables | السوق والمقارنات |
| 14_Risk Register | سجل المخاطر |
| 15_DD | Due Diligence |
| 16_IC Checklist | لجنة الاستثمار |
| 17_Negotiation | التفاوض |
| 18_Waterfall | توزيع العوائد |
| 19_Fund Ledger | الصندوق |
| 20_Audit Trail | سجل التغييرات |

**مهم جداً — تمييز بصري إلزامي بين أنواع الخلايا** (معيار Professional Underwriting):
- Hardcoded Input (مدخل يدوي)
- Calculated Output (رقم محسوب)
- Linked Formula (مربوط بمدخل/ورقة أخرى)
- External Market Data (مصدره السوق)

حتى يعرف أي محلل يفتح الملف فوراً مصدر كل رقم دون الحاجة للسؤال. (راجع مهارة `xlsx` المتاحة —
قسم "Financial models": نظام الألوان المذكور هناك أزرق=مدخلات، أسود=معادلات، أخضر=روابط بين
الأوراق، أحمر=روابط لملف خارجي، أصفر=افتراضات رئيسية — يغطي جزءاً من هذا التمييز، ويحتاج تمديداً
ليشمل "بيانات سوق خارجية" كفئة رابعة صريحة).

مثل قسم الطباعة، عدة أوراق (Sensitivity, Scenarios, Comparables, Risk Register, DD, Negotiation,
Waterfall) تعتمد على أنظمة لم تُبنَ بعد.

---

## 3) PowerPoint — من Financial Presentation إلى Investment Committee Presentation

**الوضع الحالي:** يبدأ بعنوان الفرصة/المدينة/التوصية بصيغة 16:9 — أقرب إلى Financial Presentation
Export منه إلى عرض لجنة استثمار.

**الهيكل المقترح (١٠–١٢ شريحة فقط — اللجنة لا تريد ٤٠ شريحة بيانات):**

1. **Investment Opportunity** — اسم الفرصة، الموقع، حجم الاستثمار، Equity Required، IRR، MOIC، Recommendation
2. **Executive Investment Case** — ثلاثة مربعات: Why Invest? / Why Now? / Why This Price?
3. **Asset / Location** — الموقع + البيانات الرئيسية + صورة/خريطة إن وجدت
4. **Business Plan** — Land → Development → Lease/Sale → Exit (بصرياً)
5. **Financial Summary** — TPC، Equity، Debt، Revenue، Profit، IRR، MOIC، NPV، DSCR
6. **Cash Flow** — رسم Cash Flow سنوي
7. **Sensitivity** — جدول (-10% / Base / +10%) لـ Sale Price، Construction Cost، Occupancy مع IRR وMOIC
8. **Risk Matrix** — ٥-٨ مخاطر رئيسية (Probability × Impact) مع Mitigation
9. **Market Evidence** — Comparable Transactions، Market Rent، Cap Rate، Price/m²
10. **Investment Committee Decision** — 🟢 Approve / 🟡 Approve with Conditions / 🔴 Reject + Conditions Precedent
11. **Negotiation Strategy** — Maximum Acquisition Price، Target Price، Walk-away Price (من أهم الشرائح المطلوب إضافتها)
12. **Appendix** — Detailed financial assumptions

---

## 4) إعادة صياغة خارطة الأنظمة — "عشرة محركات" (Engines) + فلسفة النظام

المستخدم أعاد صياغة رؤية النظام ككل كعشرة محركات مترابطة، بدل قائمة ميزات منفصلة. هذا يتقاطع
مع خطة المرحلة ١/٢/٣ القائمة (وليس بديلاً عنها) — الجدول التالي يربط كل محرك بالحالة الفعلية:

| # | المحرك (Engine) | الحالة | يقابل في خطة المراحل الحالية |
|---|---|---|---|
| 1 | Opportunity Engine | ✅ موجود بدرجة جيدة | Phase 1-4 السابقة |
| 2 | Pipeline Engine | ✅ **مبني ومُتحقَّق منه** | Investment Pipeline (المرحلة ١) ✅ |
| 3 | Underwriting Engine (+ Maximum Acquisition Price) | ✅ **مبني ومُتحقَّق منه** — بحث ثنائي على `core.compute()` لسعر الأرض المحقِّق للعائد المستهدف | Maximum Acquisition Price (المرحلة ٢) ✅ |
| 4 | Market Intelligence Engine | 🟡 **جزء منه مبني**: Comparable Transactions ✅ (قاعدة مقارنات + صفحة إدارة + مقارنة تلقائية حسب المدينة) وValuation Engine ✅ (٣ طرق تقييم)؛ يبقى الجزء الأوسع (إيجارات/Cap rates/تكاليف بناء السوقية كمرجع مستقل) غير مبني | Comparable Transactions + Valuation Engine (المرحلة ٢) ✅ (جزئياً) |
| 5 | Risk Engine (Probability × Impact) | ✅ **مبني ومُتحقَّق منه** — ١١ فئة | Risk Engine (المرحلة ١) ✅ |
| 6 | DD Engine (Legal/Technical/Planning/Valuation/Market/Financial/Tax/Environmental/Financing/Commercial) | ✅ **مبني ومُتحقَّق منه** — ١٠ فئات، ١٣ بنداً | Due Diligence Checklist (المرحلة ١) ✅ |
| 7 | Investment Committee Engine (قرار + أسباب + شروط) | ✅ **مبني ومُتحقَّق منه** — سجل قرارات + شروط قابلة للتتبع | IC Workflow (المرحلة ١) ✅ |
| 8 | Negotiation Engine (Asking/Underwritten/Target/Max/Walk-away + سجل عروض) | ✅ **مبني ومُتحقَّق منه** — الأسعار الخمسة معاً + IRR حي لكل سعر + سجل جولات تفاوض | Negotiation Tracker (المرحلة ٢) ✅ |
| 9 | Portfolio Engine (بعد الإغلاق: NAV/Debt/LTV/DSCR/Actual vs Budget) | 🔴 لم يُبنَ بعد | Portfolio Intelligence (المرحلة ٣) |
| 10 | Evidence & Audit Engine (Source+Date+Confidence لكل رقم) | ✅ **مبني ومُتحقَّق منه بالكامل** — Audit Trail ✅ + Source & Evidence Tracking ✅ (مصدر/تاريخ/ثقة لـ١١ رقماً رئيسياً) | Audit Trail ✅ + Source & Evidence Tracking (المرحلة ٢) ✅ |

**زيادة على الجدول أعلاه — أنظمة مُتحقَّق منها أيضاً وغير مذكورة صراحة في جدول المحركات العشرة
الأصلي**: **Data Quality Score** ✅ (نسبة اكتمال المدخلات الحرجة + بوابة منع الرفع للجنة)،
**Investment Score** ✅ (درجة مركّبة ٠-١٠٠ بالأوزان الثمانية المذكورة، مع إعادة حساب فورية عند أي
تعديل)، و**Scenario Manager** ✅ (سيناريو ضغط Stress قابل للتخصيص، فوق السيناريوهين المتفائل/
المتشائم الجاهزين مسبقاً في `core.js`) — و**تحليل الحساسية (Sensitivity)** تبيّن أنه **مبني مسبقاً
وبشكل شامل** داخل `core.js` (`sensitivityRows()`: سعر الأرض، سعر البيع/الإيجار، معدل الرسملة عند
الخروج، السايبور، LTC، الهامش، تكلفة/مدة/احتياطي البناء، الإشغال، OPEX، ونمو/تكلفة حمل الأرض
للـLandbank) — لم يكن بحاجة لبناء من الصفر.

**🎉 المرحلة ١ اكتملت بالكامل (٧/٧): Audit Trail، Pipeline، Due Diligence، Data Quality Score،
Risk Engine، Investment Score، IC Workflow — جميعها مبنية ومُتحقَّق منها آلياً (Playwright)، مع
تكامل حقيقي بين الأنظمة (مثال: أي حفظ من Pipeline/DD/Risk/Score/IC يُسجَّل تلقائياً في Audit
Trail دون أي كود إضافي؛ قسم IC يعرض ملخص جاهزية حي من Data Quality وDue Diligence مباشرة).**

**🎉 المرحلة ٢ اكتملت بالكامل (٧/٧، بالعدّ على أساس البنود الأصلية للمستخدم): Maximum Acquisition
Price، Negotiation Tracker، Scenario Manager، Comparable Transactions، Valuation Engine، Source
& Evidence Tracking — جميعها مبنية ومُتحقَّق منها آلياً (Playwright، بما في ذلك اختبار تكامل مُجمَّع
للأنظمة الـ١٣ معاً بعد إضافة آخر نظام)، بالإضافة لاكتشاف أن تحليل الحساسية (البند السابع) كان مبنياً
مسبقاً وبشكل شامل. كل الأنظمة تُعيد استخدام `core.compute()` مباشرة (بحث ثنائي على أسعار تجريبية —
Max Acquisition Price/Negotiation/Scenario Stress/Valuation DCF) دون أي تكرار لمنطق `core.js`
المالي، وكلها تتكامل تلقائياً مع Audit Trail عبر `registerBeforeOpportunitySave`. القاعدة الأرشيفية
(`window.opportunities` وكل صادرات `core.js` الأخرى) أُصلحت لتكون خاصيات حيّة (getters) بدل نسخة
جامدة عند التحميل — خلل معماري مهم اكتُشف وأُصلح خلال هذه المرحلة. جارٍ الآن الانتقال للمرحلة ٣.**

**🎉 المرحلة ٣ اكتملت بالكامل (٨/٨): Portfolio Intelligence، Concentration Risk، Benchmark Engine
(مكتبة أوبال المرجعية)، OPAL Investment Command Center، Alerts & Tasks، AI Investment Analyst،
Automated IC Memo، Data Room — جميعها مبنية ومُتحقَّق منها آلياً (Playwright، بما في ذلك اختبار
تكامل مُجمَّع للأنظمة الـ١٩ معاً على كلتا فرصتَي العرض التجريبي، وكل شاشات Main View الخمس تُفتح
وتُغلَق بلا أخطاء). أبرز ما بُني:
- **Portfolio Intelligence**: AUM، رأس المال المستثمر/غير المستثمر (Dry Powder)، NAV، Gross IRR
  (مرجَّح بحقوق الملكية) وNet IRR (حساب XIRR حقيقي بتواريخ فعلية لنداءات رأس المال والتوزيعات +
  NAV الحالي كتدفق ختامي — منهجية "Since-Inception IRR" المؤسسية القياسية)، TVPI، الدين وLTV
  وDSCR، وتركّز المحفظة حسب الصندوق/المدينة/النوع.
- **Concentration Risk**: بانر تحذيري تلقائي (حدّ ٣٥٪) + ملاحظة سياقية داخل كل فرصة.
- **OPAL Benchmark Library**: قاعدة معايير سوق قابلة للتحرير من الواجهة (مدينة×نوع → نطاقات
  IRR/Cap Rate/DSCR/YoC وأسعار مرجعية) + قسم "الفرصة مقابل المعيار المرجعي" في كل فرصة.
- **Command Center**: شاشة تجميعية واحدة (Pipeline/Capital/IC/Returns/Alerts) — تجميع عرضي بحت
  للأنظمة القائمة، بلا أي حساب جديد.
- **Alerts & Tasks**: تنبيهات تلقائية (DD حرجة، DSCR/IRR دون الحد الأدنى، إجراءات Pipeline متأخرة،
  IC معلَّقة، اقتراب سعر التفاوض من الانسحاب).
- **AI Investment Analyst**: توليف نصي (Rule-based) من كل الأنظمة الموثَّقة — بلا استدعاء نموذج
  ذكاء اصطناعي خارجي فعلي (لا توجد بنية تحتية خلفية/مفتاح API في هذا المشروع الثابت المنشور على
  GitHub Pages لدعم ذلك بأمان) وبلا أي رقم غير موجود مسبقاً في التطبيق — يفي حرفياً بطلب المستخدم
  "مقيَّد باستخدام بيانات التطبيق الموثَّقة فقط".
- **Automated IC Memo**: ملخص تنفيذي آلي دائم الظهور (شاشة + طباعة)، يظهر أولاً بين الأقسام
  الإضافية بترتيب تسجيل مدروس في main.js.
- **Data Room**: بيانات وصفية للمستندات (لا تخزين ملفات فعلي — لا Firebase Storage مُهيَّأ لهذا
  المشروع).

خلل معماري إضافي اكتُشف وأُصلح خلال هذه المرحلة (في ملفات الميزات فقط، لا `core.js`): بطاقات
"فتح التفاصيل" داخل Kanban (`pipeline.js`) ومركز القيادة (`command-center.js`) كانت تستخدم
action المدمج `open-detail` الذي يضبط `openDetailId` لكن لا يُصفِّر `mainView` — فيستمر العرض
الرئيسي النشط (Kanban/Command Center) بتجاوز عرض التفاصيل بصرياً رغم ضبط المعرّف بشكل صحيح. تم
الإصلاح باستخدام actions مخصَّصة (`pipeline-view-opp`/`cc-view-opp`) تُصفِّر `mainView` صراحة قبل
فتح التفاصيل — حل ضمن ملفات الميزات فقط، بلا أي لمس لـ`core.js`.

**فلسفة النظام المطلوبة:** الانتقال من نظام يسأل "ما بيانات الفرصة؟" إلى نظام يسأل بالتسلسل:
هل نستثمر؟ ← كم ندفع؟ ← ما الذي قد يجعلنا نخسر؟ ← ما الذي يجب التحقق منه؟ ← ما شروط الموافقة؟ ←
(بعد الاستثمار) هل الأداء الفعلي يطابق الـUnderwriting؟ — أي التحول من Real Estate Database إلى
Investment Management System.

**اقتراح المستخدم البديل:** بناء Blueprint هندسي كامل للنظام (الشاشات + الوحدات + دورة حياة الفرصة
+ قواعد الانتقال + قاعدة البيانات + الصلاحيات + محرك المخاطر + DD + IC + التفاوض + Portfolio +
شكل Excel/PDF/PPT) **قبل** الاستمرار في تعديل `src/`، بدل الاستمرار في البناء التدريجي. **القرار
المتّخذ:** المستخدم افتتح رسالته بـ"استكمل، النظام التالي" — فالتنفيذ يستمر بالنمط التدريجي الحالي
(نظام كامل، مُتحقَّق منه، مُسلَّم، ثم التالي) بدل التوقف لكتابة Blueprint منفصل، لأن هذا النمط أثبت
نجاحه فعلياً (Pipeline + Audit Trail سُلِّما بجودة عالية وبتكامل حقيقي بينهما دون تخطيط مسبق موثّق).
إن رغب المستخدم صراحة بالتوقف لكتابة Blueprint شامل قبل الاستمرار، فهذا قرار يستحق تأكيداً صريحاً
منه أولاً نظراً لحجم العمل الذي يمثله (وثيقة مستقلة ضخمة قبل أي كود جديد).

---

## ترتيب التنفيذ المقترح لهذا الملف تحديداً

**المراحل الثلاث اكتملت بالكامل (٢١ نظاماً/بنداً: ٧ في المرحلة ١ + ٧ في المرحلة ٢ + ٨ في المرحلة
٣، بالعدّ على أساس بنود المستخدم الأصلية، شاملاً اكتشاف تحليل الحساسية الجاهز مسبقاً).** كل بيانات
هذا الملف متاحة الآن فعلياً لإعادة البناء الموصوفة أعلاه (أقسام IC Checklist/Negotiation
Strategy/Comparables/Sensitivity/Scenarios/Risk Register/DD Status في التقرير المطبوع، وأوراق
Sensitivity/Scenarios/Comparables/Negotiation/Waterfall/Risk Register/DD/IC Checklist في Excel،
وشرائح Sensitivity/Risk Matrix/Market Evidence/Negotiation Strategy في PowerPoint — كل هذه الآن
لديها بيانات حقيقية تُبنى عليها)، بدل قسم منها فارغ من البيانات. الترتيب الفعلي المتبقي:
1. ✅ ~~المرحلة ٣ (الأنظمة الثمانية أعلاه)~~ — اكتملت.
2. إعادة بناء التقارير الثلاثة:
   - ✅ **الطباعة (Print IC Book, ٢١ قسماً)** — اكتملت. طريقة عرض رئيسية جديدة ومستقلة تماماً
     (`src/features/ic-book-print.js`, `registerMainView('icBook', ...)`) — صفر تعديل على
     `renderDetail()` الحالية في core.js (بقيت كما هي، لا تزال تعمل كمذكرة سريعة). زر
     "📘 طباعة كتاب اللجنة الكامل (٢١ قسماً)" يظهر أسفل الملخص التنفيذي الآلي في كل فرصة.
     الأقسام الـ٢١ بالترتيب المطلوب بالضبط (Cover → ... → Sources/Evidence/Disclaimer)، كل رقم
     فيها معاد استخدامه من دوال موجودة مسبقاً (compute/sensitivityRows/scenarioCompareRows/
     icRecommendations/dealStrengthsAndNegotiationPoints/generateAnalystNarrative/ddStats/
     RISK_CATEGORIES/matchBenchmarks+aggregateBench/maxAcquisitionPrice/د.negotiation/
     د.ic.decisions/د.evidence) — صفر حسابات مالية جديدة. نفس أصناف CSS الموجودة أصلاً
     (.memo/.section/.kv/table.db/.n) ونفس زر الطباعة (`data-action="print-memo"`)، فتظهر
     بنفس جودة الطباعة/PDF الحالية دون أي CSS جديد. اختُبرت عبر Playwright على الفرصتين
     التجريبيتين (سكني ومكتبي): صفر PAGE ERRORS، القيم المرجعية (equityIRR/MOIC) لكل من
     compute() قبل/بعد الفتح والإغلاق مطابقة بالضبط لخط الأساس الموثَّق، الأقسام ٢-٢١ (بالترقيم
     الرقمي — صفحة الغلاف "١" عنوان بلا ترقيم رقمي بتصميم مقصود) ظاهرة في الـHTML لكلتا الفرصتين،
     ولقطة شاشة في وضع الطباعة (`page.emulateMedia({media:'print'})`) تؤكد تنسيقاً احترافياً سليماً
     (رأس متكرر، شعار، شارة توصية، جداول ملوَّنة موجبة/سالبة، ترقيم أقسام واضح).
   - ✅ **Excel — دفتر الاكتتاب الاستثماري الكامل (٢١ ورقة)** — اكتمل. تصدير مستقل تماماً
     (`src/features/excel-workbook.js`, `registerExcelWorkbook`) — صفر تعديل على
     `exportOpportunityExcel()` الحالية في core.js (بقيت كما هي، لا تزال تعمل كتصدير سريع من
     زر "⬇️ Excel" الأصلي). زر "📊 تنزيل دفتر الاكتتاب الكامل (Excel، ٢١ ورقة)" جديد أسفل
     الملخص التنفيذي الآلي. الأوراق الـ٢١ بالأسماء والترتيب المطلوبين بالضبط (`00_IC Dashboard`
     حتى `20_Audit Trail`)، مبنية بالكامل على مساعدات Excel الموجودة مسبقاً في core.js (`XL`,
     `xlBorderAll`, `xlColLetter`, `xlRowsBuilder`, `xlNewSheet`, `xlSetFormula`) — Freeze
     Panes وPrint Area وZebra rows وترقيم الصفوف حسب نوعها (title/section/header/note/data) كلها
     موروثة تلقائياً. تلوين الخلايا الإلزامي بأربع فئات (مُدخَل يدوي أزرق FF1E40AF / نتيجة محسوبة
     أسود افتراضي / رابط لصيغة أخضر FF15803D / بيانات سوقية خارجية كَرَم FEF3C7 بخط FF92650B)
     مُطبَّق عبر دالة `colorize()` جديدة فوق كل ورقة. معادلات Excel حقيقية غير مُحسَبة سلفاً في
     جافاسكريبت: TPC=SUM(B4:B9)، Equity=TPC-Debt، وفي أوراق التدفقات النقدية IRR(range)/NPV(...)
     /SUMIF(...) تُشير فعلياً لخلايا الورقة لا لقيم جاهزة. جميع البيانات مصدرها دوال/بيانات
     موجودة مسبقاً (compute، ddStats، RISK_CATEGORIES، matchBenchmarks+aggregateBench،
     maxAcquisitionPrice، fundLedgerSummary، سجل التدقيق) — صفر حسابات مالية جديدة. اختبار
     Playwright الحقيقي عبر المتصفح تعذَّر بسبب قيد بيئي في هذه البيئة السحابية (الـPlaywright
     Chromium المعزول لا يستطيع تحميل سكربتات ExcelJS من الشبكات الخارجية cdn.jsdelivr.net/
     cdnjs.cloudflare.com عبر البروكسي المُهيَّأ لها — قيد شبكي في البيئة وليس خللاً في الكود؛
     التصدير السريع الأصلي في core.js يشترك في نفس القيد لنفس السبب). بديلاً، بُني اختبار تنفيذ
     فعلي عبر Node.js + jsdom (`/tmp/test_xlbook_node.mjs`): يُحاكي واجهة ExcelJS الفعلية
     المُستخدَمة فقط (Workbook/Worksheet/Row/Cell)، ثم يستورد `core.js` و`excel-workbook.js`
     كوحدات ES حقيقية ويستدعي معالج الحدث الفعلي مباشرة — هذا يُشغِّل محرك core.js المالي الحقيقي
     (`compute()`, `withDefaults()`, ...) فعلياً، لا مجرد تحقق من بدء التنزيل. النتيجة: صفر أخطاء
     مرمية عبر أنواع الفرص الثلاثة (income/development/landbank)، جميع الأوراق الـ٢١ مبنية لكل
     نوع، وفحص دقيق لخلايا/معادلات/ألوان محددة (ورقة `03_Sources & Uses`، لون خلية مُدخَل في
     `01_Opportunity`، عدد صفوف `13_Comparables`) أكَّد صحتها جميعاً.
   - ✅ **PowerPoint — عرض لجنة الاستثمار (١٢ شريحة)** — اكتمل. تصدير مستقل تماماً
     (`src/features/ic-presentation.js`, `registerICPresentation`) — صفر تعديل على
     `exportOpportunityPptx()` الحالية في core.js (بقيت كما هي، لا تزال تعمل كتصدير سريع من زر
     "⬇️ PowerPoint" الأصلي). زر "🖥️ تنزيل عرض لجنة الاستثمار (PowerPoint، ١٢ شريحة)" جديد أسفل
     الملخص التنفيذي الآلي. الشرائح الـ١٢ بالترتيب المطلوب بالضبط: (١) الفرصة الاستثمارية، (٢)
     الحالة الاستثمارية التنفيذية (لماذا الاستثمار/لماذا الآن/لماذا هذا السعر — من بيانات محسوبة
     فعلياً عبر `generateAnalystNarrative`/`matchBenchmarks`+`aggregateBench`/
     `maxAcquisitionPrice`)، (٣) الأصل والموقع، (٤) خطة العمل (٤ مراحل حسب نوع الفرصة)، (٥)
     الملخص المالي، (٦) التدفقات النقدية (جدول من `c.projectCF`/`c.equityCF`)، (٧) تحليل
     الحساسية (`core.sensitivityRows`)، (٨) مصفوفة المخاطر (أعلى ٧ مخاطر حسب الدرجة)، (٩) أدلة
     السوق (المقارنات + المعايير المرجعية)، (١٠) قرار لجنة الاستثمار (التوصية + جدول الشروط
     المسبقة)، (١١) استراتيجية التفاوض (٤ مربعات: سعر الطلب/المستهدف/الحد الأقصى للاستحواذ/
     الانسحاب)، (١٢) الملحق (الافتراضات + إخلاء المسؤولية). نفس نظام الألوان المستخدم أصلاً في
     `exportOpportunityPptx` (أخضر 0E6B4C، نص 4C5850، حبر 152019...) وأصناف PptxGenJS نفسها
     (`addSlide`/`addText`/`addTable`/`writeFile`، تخطيط WIDE 13.33×7.5). صفر حسابات مالية جديدة
     — كل رقم مصدره دوال/بيانات موجودة مسبقاً. نفس قيد الشبكة البيئي منع اختبار Playwright
     الحقيقي عبر المتصفح (PptxGenJS من نفس الـCDN المحجوب). بديلاً، بُني اختبار Node.js + jsdom
     مطابق لنفس المنهجية (`/tmp/test_pptbook_node.mjs`) يُحاكي واجهة PptxGenJS الفعلية
     المُستخدَمة فقط (`addSlide`→`{addText, addTable}`, `writeFile`)، مع تعطيل `alert` (يُحوَّل
     إلى استثناء) حتى لا يمر أي خطأ صامتاً. النتيجة: صفر أخطاء مرمية عبر أنواع الفرص الثلاثة،
     بالضبط ١٢ شريحة (`addSlide()`) لكل نوع، وفحص محتوى الشرائح (الشريحة ١ تحمل اسم الفرصة،
     الشريحة ٦ "التدفقات النقدية" تحوي جدولاً بـ٥ صفوف) أكَّد الترتيب والمحتوى الصحيحين.
3. ✅ **التسليم النهائي** — جاهز الآن. التقارير الثلاثة (Print/Excel/PowerPoint) والمراحل الثلاث
   (٢١ نظاماً) جميعها مكتملة ومُتحقَّق منها. سيُسلَّم ملف مضغوط واحد فقط عبر SendUserFile، حسب
   طلب المستخدم الصريح بعدم التسليم إلا مرة واحدة بعد اكتمال كل شيء.

---

# المرحلة الرابعة (٩ سبتمبر ٢٠٢٦) — الأدوار والصلاحيات + إثراء المكتبات المرجعية

طلب المستخدم ستة بنود متتالية (طلب الانتظار مرتين قبل إرسال القائمة الكاملة عمداً، حتى تُجمَع
كل الأفكار قبل البدء بالتنفيذ). البنود الستة، وحالة كل منها الآن:

1. ✅ **تقاطع تحقق سعر البيع (Cap Rate × السعر المباشر)** — `benchmark-engine.js`. أُضيف معيار
   ثانٍ مستقل لاستنتاج سعر البيع/م² عبر منهج رسملة الدخل (Income Capitalization):
   السعر المُشتق = الإيجار/م² ÷ معدل الرسملة الأوسط، ويُقارَن بسعر البيع/م² المُدخَل مباشرة في
   نفس السجل المرجعي — شارة تطابق ثلاثية (🟢 فرق ≤١٠٪ / 🟡 ١٠-٢٠٪ / 🔴 >٢٠٪) في عمود جديد بجدول
   إدارة المكتبة. بهذا صار لكل سعر مرجعي في المكتبة معياران مستقلان للتحقق من دقته.

2. ✅ **مكتبة الكفاءة المعمارية والتكلفة المرجعية** — `space-efficiency-data.js` (١٥٣ سجلاً،
   البيانات الخام التي زوَّدنا بها المستخدم، مُفكَّكة برمجياً ومُصنَّفة قطاعياً) +
   `space-efficiency-library.js`. أُثريت المكتبة بخلاصة مهنية مكثَّفة لكل قطاع (١٤ قطاعاً:
   سكني، مكاتب، تجزئة، ضيافة، لوجستي، مراكز بيانات، صحي، تعليمي، ترفيهي، مواقف، بنية تحتية،
   موقع عام، محطات وقود، مرافق) — نسب كفاءة GLA/BUA مرجعية، معايير مواقف، WALE، هامش NOI، حساسية
   معدل الرسملة، ملاحظات مناخية/تسويقية — حسب طلب المستخدم الصريح بتكثيف المعرفة العقارية.
   تُعرَض كـ"مؤشرات معمارية خفيفة" (حسب قرار المستخدم: لا تغيير كامل في نموذج البيانات الآن) في
   ثلاث نقاط: لحظة اختيار نوع الاستخدام الرئيسي بالمعالج (خطوة ٤)، صفحة الفرصة المحفوظة، ومكتبة
   كاملة قابلة للتصفح والتصديق (نفس نمط الأدوار/الصلاحيات). تجاوزات/إضافات محلية للفريق
   (`spaceEffOverrides`) قابلة للتصديق من مدير الصندوق فأعلى — السجلات الأساسية الـ١٥٣ ثابتة في
   الكود (مرجع مؤسسي، بلا استدعاء شبكة).

3. ✅ **قنوات الخروج المؤسسية** — `exit-channels.js`. تمييز مهم وُضِّح للمستخدم: `EXIT_STRATEGIES`/
   `holdStrategy` الموجودان أصلاً في core.js هما "شكل التخارج على مستوى الأصل" (بيع/تأجير/إعادة
   تمويل) ويدخلان فعلياً في `compute()` — لم يُلمَسا. أُضيف حقل جديد تماماً `exit.channel`
   ("قناة/جهة الخروج") بست قيم: بيع مباشر لمشتر واحد (الافتراضي) / بيع بالجملة لصندوق REIT /
   بيع جزئي بالوحدة (Strata) / إعادة تمويل والاحتفاظ (وثيقة نية — التأثير الحسابي الفعلي عبر
   holdStrategy) / تحويل لصندوق مُدرَج (IPO) / إغلاق الصندوق وبيع كامل الوحدات (السادسة، مُقترَحة
   من فريق التنفيذ). كل قناة: علاوة/خصم نمطي على معدل الرسملة، تكلفة صفقة، جدول زمني، وخلاصة
   مهنية — تُعرَض كـ"تحليل استكشافي" (Indicative Overlay) بجانب الرقم الرسمي دون تعديله.

4. ✅ **رافعات الجدوى القابلة للتفعيل** — `feasibility-levers.js`. خمس رافعات حقيقية (لا نص وصفي):
   تقليص احتياطي الطوارئ، زيادة الرافعة المالية (LTC) ضمن هامش DSCR، تمديد مدة استهلاك الدين،
   رفع الإيجار لمطابقة المعيار المرجعي (يُغذَّى من بند ١ أعلاه مباشرة)، تقليص دور بدروم. كل رافعة
   تظهر فقط عند انطباق شرطها الفعلي على بيانات الفرصة، وتُعرِض مقارنة Equity IRR/MOIC/DSCR
   محسوبة حقيقياً (عبر `core.compute()` على نسخة تجريبية) قبل التطبيق — وزر "تطبيق" يكتب التعديل
   فعلياً على الفرصة (`core.persistOpportunity`) ويُعاد حساب كل شيء فوراً، بصلاحية `canEditOpp`
   فقط. يُسجَّل تلقائياً في سجل التدقيق (نفس آلية الحفظ العادية).

5. ✅ **مكتبة منحنيات التوزيع الإحصائي** — `cost-distribution-library.js`. ٨ بنود مرجعية (سعر
   الأرض، تكلفة البناء، معدل الرسملة، سعر الفائدة، الإشغال، تصاعد الإيجار/البيع، استخدام
   الطوارئ الفعلي، مدة الإنشاء) — لكل بند: المنحنى الإحصائي الأنسب (PERT/Lognormal/Beta/Normal
   مقيَّد...) وسببه الفني، بديل مقبول عند نقص البيانات، وطريقة معايرة عملية بثلاث نقاط. مؤشر
   تثقيفي بالكامل يظهر في خطوات المعالج ذات الصلة (١، ٤، ٨) ومكتبة كاملة — بلا محاكاة Monte
   Carlo فعلية، حسب النطاق المتفَق عليه.

6. ✅ **طبقة الحماية والأدوار (الأولوية الأولى في الترتيب الفعلي)** — `roles-permissions.js` +
   تحديثات `firestore.rules`. أربع فئات (محلل ← عضو لجنة استثمار أول ← مدير صندوق ← أدمن)، كل
   فئة تشمل صلاحيات ما قبلها. إدارة/تصديق كل المكتبات المرجعية (Benchmark، الكفاءة المعمارية)
   محصورة بمدير الصندوق فأعلى — واجهياً (إخفاء الأزرار) **وفعلياً** (قواعد Firestore الجديدة
   `isFundManagerOrAbove()`). ⚠️ **اكتُشف وأُصلِح كجزء من هذا العمل**: تسع مجموعات بيانات
   (`investors/funds/commitments/capitalCalls/distributions/transactions/comparables/
   benchmarks/oppAuditLog`) كانت بلا أي قاعدة أمان في `firestore.rules` منذ إضافتها — أي مرفوضة
   بالكامل تحت أي Firebase حقيقي (غير وضع الديمو المحلي) — لم يظهر هذا في أي اختبار سابق لأن كل
   اختبارات Playwell السابقة استخدمت `?demo=1` (محلي بالكامل، لا يلمس Firestore الحقيقي إطلاقاً).
   أُضيفت القواعد الناقصة كجزء من نفس عمل هذه المرحلة.

**اكتشاف تقني مهم أثناء بناء الأدوار (يُسجَّل هنا لتجنّب تكراره مستقبلاً):** أي `<select>` جديد
مرتبط بـ`data-action` مخصّص **لن يعمل أبداً** — مُستمع `change` في `core.js` مكتوب حصراً لثلاث
حالات محدَّدة (`filter-city`/`filter-type`/`branding-logo-file`) ولا يستدعي معالجات الإجراءات
الخارجية (`runExternalActionHandlers`) لأي إجراء آخر. الحل المُعتمَد في كل هذه المرحلة: أزرار
نقر (`<button data-action="...">`) دائماً بدل القوائم المنسدلة لأي تفاعل مخصّص جديد.

**التحقق من البناء:** كل الملفات الجديدة/المُعدَّلة (`roles-permissions.js`،
`benchmark-engine.js`، `space-efficiency-data.js`، `space-efficiency-library.js`،
`exit-channels.js`، `feasibility-levers.js`، `cost-distribution-library.js`، `main.js`،
`firestore.rules`) فحصت بـ`node --check` (صفر أخطاء صياغة عبر كل ملفات `src/`)، وباختبارات
Node.js وظيفية مخصَّصة لكل نظام (محاكاة `core` بكل نقاط التوسّع المستخدَمة) + اختبار تكاملي
مُصغَّر لكل الوحدات الستة معاً (تسجيل بلا تعارض في مفاتيح `registerMainView` أو الإجراءات) —
كل الاختبارات نجحت (`ALL PASSED`). لم يُتحقَّق عبر متصفح حقيقي (Playwright) بسبب نفس قيد الشبكة
البيئي المذكور في المرحلة السابقة لهذا المشروع.

## أفكار إثراء إضافية مُقترَحة من فريق التنفيذ (لم تُبنَ بعد — للمرحلة القادمة إن رغب المستخدم)

- **مكتبة الالتزام التنظيمي (Zoning/FAR-BAR Compliance)** — مطابقة تلقائية بين `land.far`/
  `land.bar` المُدخَلين والحدود النظامية المعروفة لكل بلدية/تصنيف أرض، مع تنبيه عند التجاوز.
- **معيار حساسية WALE/تصنيف ائتمان المستأجر على معدل الرسملة** — ربط `CREDIT_TIERS` الموجودة
  أصلاً في core.js بجدول تأثير كمي على معدل الرسملة المرجعي (مستأجر استثماري يستحق رسملة أضيق).
- **مؤشر "دقة الاكتتاب" على مستوى المحفظة** — عبر `portfolio.js`: مقارنة تراكمية بين الافتراضات
  المُدخَلة وقت الاكتتاب والنتائج الفعلية بعد التنفيذ (تغذية عكسية تُحسِّن دقة الافتراضات المستقبلية).
- **منحنيات تصاعد تكلفة البناء الإقليمية** — مرجع تاريخي لمعدل تضخم مدخلات البناء في السعودية
  (حديد/أسمنت/عمالة) لكل سنة، يُغذّي معايرة بند "تكلفة البناء" في مكتبة منحنيات التوزيع (بند ٥).

# المرحلة الخامسة (٩ سبتمبر ٢٠٢٦)

طلب المستخدم بعد استلام تسليم المرحلة الرابعة (رضاه: "ممتاز جداً"): أمران في رسالة واحدة —
(أ) إضافة كل خبرة الاستثمار/التطوير العقاري المتعلقة بالاستخدامات والكفاءات **والمصاريف**
تحديداً إلى قواعد البيانات بشكل احترافي، و(ب) خريطة تفاعلية للمملكة العربية السعودية تُظهر
الفرص بعناوينها، قابلة للنقر/التقريب لعرض بيانات الفرصة وتفاصيلها في الداشبورد. القراران
المُلزِمان المُتَّخَذان عبر AskUserQuestion: (أ) **"كلاهما"** — مكتبة جديدة مخصّصة للرسوم/
المصاريف + إغناء المكتبات القائمة، لا أحدهما فقط؛ (ب) **إضافة حقل موقع دقيق (Lat/Lng) لكل
فرصة** — تغيير حقيقي في نموذج البيانات (لا الاعتماد على مركز المدينة فقط كما في الخيار الأسهل
المطروح).

## ١. مكتبة رسوم الصندوق والمصاريف التشغيلية المرجعية (`fund-fees-opex-library.js`، جديد)

مكتبة مرجعية بنفس فلسفة `benchmark-engine.js`/`space-efficiency-library.js` لكن على مستوى
"اقتصاديات الصندوق" لا مستوى الأصل — **لا تُضيف أي حساب جديد**؛ كل الحقول التي تُقارَن بها
(`fees.mgmt/structuring/arrangement/acquisition/disposition/assetMgmt/propMgmt`،
`economics.hurdle/carry`) وبنية توزيع الأرباح الكاملة (Waterfall: `PIC/roc/pref/catchup/
carryPool/lpBonus/gpManager/devPromote/lpTotal/gpTotal/devTotal`) موجودة ومُحسَبة فعلاً في
`core.js` — الجديد هنا هو نطاقات مؤسسية معتادة (Benchmark) لكل حقل + شرح مراحل الـWaterfall
الأربع بالأسماء والأرقام الحقيقية + نطاقات OPEX مرجعية حسب القطاع (`OPEX_BY_SECTOR`، مُصدَّرة).

- **٩ بنود رسوم مرجعية** (`FEE_BENCHMARKS`): رسوم الإدارة (١-٢٪ سنوياً)، الهيكلة (٠.٥-١.٥٪)،
  ترتيب التمويل (٠.٥-١٪ من الدين)، الاستحواذ (١-٢٪)، البيع/الخروج (٠.٥-١.٥٪)، إدارة الأصول
  (٠.٥-١٪ سنوياً)، إدارة العقارات (٣-٦٪ من الإيجار)، معدل العائد المستهدف Hurdle (٦-١٠٪)، حصة
  الأداء Carry (١٥-٢٠٪) — كل بند مرتبط بـ`path` حقيقي يُقرأ ويُقارَن فعلياً بقيمة الفرصة.
- **شرح Waterfall الحقيقي بأربع مراحل** (`WATERFALL_TIERS`): استرداد رأس المال (ROC) ← العائد
  المفضّل (Pref/Hurdle) ← استرداد المدير (GP Catch-up، ١٠٠٪ حتى يصل لنسبة الـCarry المتفَق
  عليها) ← التوزيع النهائي (Residual Split حسب `lpShare/gpShare/devShare`) — تُعرَض بالأرقام
  الفعلية المحسوبة (`c.roc`, `c.pref`, ...) على صفحة الفرصة المحفوظة.
- **نطاق OPEX مرجعي لكل قطاع من ١٤ قطاعاً** (`OPEX_BY_SECTOR`، بنفس مفاتيح `SECTORS` في
  `space-efficiency-data.js`): من ١٠-٢٠٪ للمستودعات/اللوجستي (الأدنى — عقود NNN) إلى ٥٥-٧٠٪
  للضيافة (الأعلى — يعادل هامش GOP ٣٠-٤٥٪ فقط)؛ القطاعات الداعمة بلا إيراد مستقل (بنية تحتية،
  مرافق) بلا نطاق (`null`) عمداً — لا نطاق OPEX منطقي لقطاع لا يُنتج إيراداً مستقلاً أصلاً.
- ثلاث نقاط عرض بنفس نمط كل مكتبات المرحلة الرابعة: مؤشر لحظي في خطوة "رسوم الصندوق" (خطوة
  ٧)، قسم تفصيلي على صفحة الفرصة (يشمل Waterfall بالأرقام الحقيقية + نطاق OPEX لقطاع الفرصة)،
  و Main View كاملة بثلاث تبويبات (رسوم/Waterfall/OPEX) + طبقة تجاوزات محلية (`fundFeeOverrides`
  في Firestore) محمية بـ`canManageLibraries` (مدير صندوق فأعلى)، بنفس نظام الأدوار من المرحلة
  الرابعة تماماً — بلا أي تكرار أو نظام صلاحيات جديد.

## ٢. إغناء المكتبات القائمة بالمصاريف (تنفيذ الشق الثاني من قرار "كلاهما")

- **`space-efficiency-library.js`**: استيراد أحادي الاتجاه لـ`OPEX_BY_SECTOR` من المكتبة
  الجديدة (لا استيراد عكسي أبداً — `fund-fees-opex-library.js` لا يستورد من هنا، فلا تعارض
  استيراد دائري) — سطر "نسبة المصاريف التشغيلية المرجعية" يظهر الآن بجانب مؤشرات الكفاءة
  المعمارية في: مؤشر لحظة الإدخال (خطوة ٣)، قسم تفاصيل الفرصة (تلقائياً عبر نفس المؤشر)، وصندوق
  الخلاصة المهنية عند تصفح/فلترة قطاع في المكتبة الكاملة.
- **`cost-distribution-library.js`**: بندان جديدان بالكامل بنفس نمط البنود الثمانية السابقة
  (شكل التوزيع الإحصائي/سببه/البديل المقبول/طريقة المعايرة): **نسبة المصاريف التشغيلية**
  (`opex_ratio`، خطوة٤، توزيع Beta — نسبة محصورة [٠٪،١٠٠٪] كالإشغال بالضبط) و**استنزاف رسوم
  الإدارة التراكمي** (`mgmt_fee_drag`، خطوة٧ الجديدة على حلقة عرض المكتبة — رسم تعاقدي شبه-حتمي
  لا عشوائي، لكنه يستحق توعية المحلل بأثره التراكمي على مدى سنوات الاستثمار على IRR/MOIC
  النهائيين). المكتبة الآن ١٠ بنود مرجعية بدل ٨، تُعرَض على أربع خطوات معالج بدل ثلاث (١،٤،٧،٨).

## ٣. خريطة الفرص العقارية (`opportunities-map.js`، جديد) — Leaflet.js + OpenStreetMap

- **حقل جذر جديد كلياً `geo`** (`{lat, lng, source}`) عبر `registerOpportunitySchemaExtender`
  — تعمّدنا **عدم** وضعه تحت `meta.geo`: `registerOpportunitySchemaExtender` في `core.js` يدمج
  بـ`Object.assign` على مستوى الجذر فقط (سطحي)، فوضعه تحت `meta` كان سيستبدل `meta` بالكامل
  (بما فيها `city`/`name`/`oppType`/... الموجودة) بدل دمجه معها — درس هندسي مهم اكتُشف قبل كتابة
  أي كود فعلي (بمراجعة كل الامتدادات الأخرى الموجودة: كلها تستخدم مفاتيح جذر فريدة لهذا بالضبط).
- **حقلا معالج عاديان** (`geo.lat`/`geo.lng`، عبر `core.F.num`) في خطوة "الهوية والموقع" (خطوة
  ٠، حيث توجد `meta.city` أصلاً) — يعملان تلقائياً بالكامل بلا أي معالج إجراء مخصّص (مستمعا
  `input`/`change` العامان في `core.js` يربطان أي حقل بمسار `name="مسار.منقّط"` داخل
  `#wizard-modal` تلقائياً) + زر مساعد "استخراج الموقع" يحلّل رابط خرائط جوجل مُلصَق (بأي صيغة
  شائعة: `@lat,lng`، `!3d/!4d` الأدق لموقع الدبوس الفعلي، `q=lat,lng`، أو رقمين مباشرين) ويعبّئ
  الحقلين تلقائياً عبر `core.setPath(core.wizard.draft, ...)`.
- **موقع افتراضي بلا ترحيل بيانات إجباري**: فرصة بلا `geo` دقيق تظهر عند مركز مدينتها تقريبياً
  (`CITY_COORDS`، ٧ مدن مطابقة لـ`CITIES` في `core.js` باستثناء "أخرى")، وبلا مدينة معروفة أصلاً
  تظهر عند مركز المملكة تقريباً معلَّمة "غير محدَّد" — كل هذا عبر دالة نقية واحدة
  (`resolveOpportunityLatLng`) قابلة للاختبار المباشر بلا أي DOM أو متصفح.
- **مشكلة تقنية حقيقية حُلَّت بلا أي لمس لـ`core.js`**: `core.js` لا يملك نقطة توسّع "بعد
  الرسم" عامة لأي `registerMainView` مخصّصة (فقط `initCharts()` الداخلية الخاصة بـChart.js)،
  و`render()` تستبدل `innerHTML` لـ`#app` بالكامل في كل استدعاء — أي عنصر Leaflet سابق يصبح
  معلَّقاً فوراً. الحل: `MutationObserver` عام على `#app` (يُسجَّل مرة واحدة عند تسجيل الوحدة)
  يتحقق بعد كل تغيير من وجود `#opp-map-canvas` ويُعيد تهيئة خريطة جديدة عند الحاجة — مع حفظ
  واستعادة مركز/تقريب الخريطة (`_lastView`) بين كل إعادة تهيئة لتقليل الإحساس بـ"الارتداد"
  البصري. محمي بالكامل بفحوصات وجود (`typeof document`/`window.L`/`MutationObserver`)، فلا
  يرمي أي استثناء في بيئة اختبار Node بلا DOM.
- **النقر/التقريب المطلوب صريحاً في طلب المستخدم**: كل دبوس (Marker) يحمل نافذة منبثقة
  (Popup) بالاسم/المدينة/نوع الاستخدام + دقة الموقع (📍دقيق / 🏙️تقريبي / ⚠️غير محدَّد) + زر
  "عرض التفاصيل الكاملة" يفتح **نفس** صفحة تفاصيل الفرصة الموجودة أصلاً في الداشبورد (لا عرضاً
  موازياً جديداً) عبر `core.setCoreState({ mainView:null, openDetailId:id, render:true })—
  بالضبط تحقيق "النقر عليها... للنظر إلى بيانات الفرصة وتفصيلها ومكوّناتها بحيث تظهر في
  الداشبورد" كما طلب المستخدم حرفياً. الخريطة نفسها Main View كاملة (زر شريط علوي "🗺️ خريطة
  الفرص") تضم أيضاً قائمة جانبية بكل الفرص (نفس زر العرض) كنسخة احتياطية تعمل حتى لو تعذَّر
  تحميل Leaflet لأي سبب شبكي.
- سكربتا/رابط Leaflet.js (`leaflet@1.9.4`) أُضيفا في `index.html` بنفس نمط سكربتات CDN
  الموجودة أصلاً (Chart.js/ExcelJS/PptxGenJS) — تحميل مباشر قبل `src/main.js`، يصبح `window.L`.

## ٤. Firestore

قاعدة جديدة على مجموعة `fundFeeOverrides` (نفس نمط `spaceEffOverrides`/`benchmarks`
بالضبط: قراءة لأي عضو مصرَّح له، كتابة/تعديل/حذف لمدير صندوق فأعلى). خريطة الفرص لا تحتاج أي
مجموعة Firestore جديدة (تقرأ فقط من `opportunities` الموجودة أصلاً، والحقل الجديد `geo` جزء
من نفس وثيقة الفرصة لا مجموعة منفصلة).

## ٥. التحقق من البناء

كل الملفات الجديدة/المُعدَّلة (`fund-fees-opex-library.js`، `opportunities-map.js`،
`space-efficiency-library.js`، `cost-distribution-library.js`، `main.js`، `index.html`،
`firestore.rules`) فُحصت بـ`node --check` (صفر أخطاء صياغة)، وباختبارات Node.js وظيفية مخصَّصة
لكل نظام جديد (بما فيها تغطية كاملة للدوال النقية في خريطة الفرص —
`resolveOpportunityLatLng`/`parseLatLngFromText` — بحتمية تامة بلا حاجة لأي DOM أو متصفح) +
إعادة تشغيل كل اختبارات المرحلة الرابعة القائمة (للتأكد من أن استيراد `OPEX_BY_SECTOR` الجديد
في `space-efficiency-library.js` لم يكسر شيئاً) + اختبار تكاملي مُصغَّر لكل الوحدات الثماني
(المرحلتين ٤+٥) معاً بنفس ترتيب `main.js` الحقيقي (لا تعارض في مفاتيح `registerMainView` أو
أسماء الإجراءات) — كل الاختبارات نجحت (`ALL PASSED`). لم يُتحقَّق عبر متصفح حقيقي (Playwright)
لأن تهيئة Leaflet الفعلية (بلاطات OpenStreetMap عبر الشبكة) تتطلب متصفحاً حقيقياً متصلاً
بالإنترنت، بنفس قيد البيئة المذكور في المرحلتين السابقتين لهذا المشروع.

# المرحلة السادسة (٩ سبتمبر ٢٠٢٦)

بعد تسليم المرحلة الخامسة، سؤال المستخدم: هل نحتاج أي شيء إضافي من خبرتنا لنجعل قاعدة البيانات
احترافية للغاية ومنافسة لأكبر البرامج المعمول بها (Argus/Yardi/MRI)؟ عبر AskUserQuestion (اختيار
متعدد)، **اختار المستخدم الأربعة المطروحة معاً** بلا استثناء: (١) مكتبة الأنظمة واللوائح
العقارية السعودية المرجعية، (٢) سجل عقود الإيجار التفصيلي (Rent Roll)، (٣) طبقة السياق
الاقتصادي الكلي على الخريطة (مشاريع رؤية ٢٠٣٠)، (٤) تصنيف الاستدامة (مستدام/ESG). الفكرة
الموجّهة للأربعة: تمييز محلي/سعودي حقيقي لا تُغطّيه البرامج العالمية الكبرى بسياق محلي واضح،
بالإضافة لعمق تحليلي (Rent Roll) تفتقده منصتنا الحالية (نسبة إشغال/إيجار إجمالية واحدة فقط).

## ١. مكتبة الأنظمة واللوائح العقارية السعودية المرجعية (`saudi-regulatory-library.js`، جديد)

مكتبة مرجعية بفلسفة `fund-fees-opex-library.js` نفسها لكن على مستوى "الأنظمة الحكومية" لا
"اقتصاديات الصندوق" — **لا تُضيف أي حساب جديد**؛ كل الحقول المُقارَنة (`exitCosts.rett`،
`exitCosts.broker`، `landbank.whiteLandFeePct`، `zakat.ratePct`، `criteria.preSaleMin`) موجودة
ومُستخدَمة فعلاً في حسابات `core.js`.

- **٥ بنود نظامية مرجعية** (`REG_ITEMS`، كل بند `kind:'fixed'` — قيمة نظامية واحدة محددة لا
  نطاقاً): ضريبة التصرفات العقارية RETT (٥٪)، عمولة السمسرة العقارية المعتادة (٢.٥٪)، رسوم
  الأراضي البيضاء (٢.٥٪ سنوياً)، تقدير الزكاة المبسّط (٢.٥٪)، والحد الأدنى للجاهزية المسبقة
  بنظام "وافي" للبيع على الخارطة (٣٠٪ توضيحياً). تُعرَض على أربع خطوات معالج (١،٤،٦،٨) حيث
  توجد الحقول المقارَنة فعلياً + قسم مقارنة كامل على صفحة كل فرصة.
- **شارة حالة غير عقابية** (`statusBadge`): تطابق = ✅، اختلاف = 🔶 "قد يكون استثناءً مشروعاً"
  — لا ❌ خطأ — لأن استثناءات نظامية مشروعة شائعة فعلاً (وراثة/هبة، تطوير فعلي، مستثمر غير
  سعودي...).
- **تحذير معلوماتي بارز** في كل مكان يظهر فيه بند: هذه معلومات مرجعية لا استشارة قانونية أو
  ضريبية رسمية، والأنظمة قد تُحدَّث — تحقّق دائماً من الجهة المختصة. نفس فلسفة تحذير `core.js`
  الحالي على رسوم الأراضي البيضاء بالضبط، مُطبَّقة الآن بشكل مركّز ومكثَّف على خمسة أنظمة.
- تجاوزات/ملاحظات الفريق المحلية (`saudiRegOverrides`، مجموعة Firestore جديدة) محمية بـ
  `canManageLibraries` (مدير صندوق فأعلى) — نفس نمط `fundFeeOverrides` بالضبط.

## ٢. سجل عقود الإيجار التفصيلي (`rent-roll.js`، جديد) — Rent Roll

نقطة قوة أساسية في Argus لا يوفّرها نموذجنا الحالي: بدل نسبة إشغال/إيجار إجمالية واحدة
(`income.occupancy`/`income.rent`)، سجل عقود إيجار فردي — عقد بعقد، بتاريخ انتهاء وتصاعد إيجار
وتصنيف ائتماني للمستأجر. **لا يُغيِّر أي حساب فعلي في `core.js`** — `income.wale`/
`income.tenantConc` المُدخَلان يدوياً يبقيان هما ما يدخل فعلياً في IRR/MOIC كما هو تماماً؛ هذا
الملف يضيف سجلاً تفصيلياً اختيارياً بجانبهما، يحسب القيم المكافئة من العقود الفعلية كـ"تحقّق
تصالبي" معلوماتي بحت (بفلسفة `priceCrosscheck` في `benchmark-engine.js`).

- **حقل جذر جديد كلياً `rentRoll.leases`** (مصفوفة) عبر `registerOpportunitySchemaExtender` —
  يُحفَظ داخل وثيقة الفرصة نفسها (نفس نمط `evidence-tracking.js` بالضبط: بيانات عملياتية خاصة
  بفرصة واحدة، لا مرجعاً مشتركاً بين الفريق) فلا حاجة لأي مجموعة Firestore جديدة.
- **صلاحية مختلفة عمداً عن باقي مكتبات هذه المرحلة**: `core.canEditOpp(rec)` لا
  `canManageLibraries` — بيانات صفقة يُفترض أن يضيفها/يعدّلها أي عضو فريق يملك صلاحية تعديل
  تلك الفرصة أصلاً، لا مكتبة مرجعية تتطلب مدير صندوق.
  دوال نقية بالكامل قابلة للاختبار المباشر: `computeWaleFromLeases` (WALE مُرجَّح بالإيجار
  على العقود السارية فقط)، `computeTenantConcentration` (أكبر عقد ÷ إجمالي الإيجار)،
  `sumLeasesGLA`، و`expirationScheduleByYear` (**جدول مخاطر التجديد** — Rollover Risk —
  تجميع العقود حسب سنة الانتهاء مع نسبة كل سنة من إجمالي الإيراد الحالي، بتحذير بصري عند تجاوز
  ٣٠٪ من الإيراد في سنة واحدة).
- تحقّق تصالبي بصري (⚠️) عند فرق ملموس بين القيمة المُدخَلة يدوياً والمحسوبة من السجل التفصيلي
  — تنبيه لتحديث الافتراض اليدوي، لا خطأ حاسم.

## ٣. طبقة السياق الاقتصادي الكلي على الخريطة (`macro-context.js`، جديد) + `geo-utils.js` (جديد)

إثراء مباشر لخريطة الفرص (`opportunities-map.js`، المرحلة الخامسة) بطبقة اختيارية (مطفأة
افتراضياً، بزر تفعيل) لمواقع أكبر مشاريع رؤية ٢٠٣٠ (`MEGAPROJECTS`: نيوم، القدية، مشروع البحر
الأحمر، أمالا، بوابة الدرعية، نيو مربع، حديقة الملك سلمان، جدة سنترال، روشن) + قسم تفصيلي على
صفحة كل فرصة يحسب فعلياً "أقرب مشروع رؤية ٢٠٣٠" (خط مباشر، صيغة Haversine) — قرب فرصة من
مشروع كبرى عامل سوقي حقيقي يرفع الطلب المتوقع على العقار في محيطه.

- **حل استيراد دائري محتمَل، مُعالَج قبل كتابة أي كود فعلي**: `opportunities-map.js` يحتاج
  بيانات `macro-context.js` (`MEGAPROJECTS`) ليرسمها كطبقة إضافية، و`macro-context.js` يحتاج
  دالة `resolveOpportunityLatLng` (المُعرَّفة أصلاً في `opportunities-map.js` من المرحلة
  الخامسة) لحساب المسافة — استيراد دائري كلاسيكي. **الحل**: استخراج كل الدوال/البيانات النقية
  المشتركة (`CITY_COORDS`، `KSA_CENTER`، `resolveOpportunityLatLng`، `parseLatLngFromText`،
  ودالة جديدة `haversineKm`) إلى ملف "ورقة" مستقل بلا أي استيراد (`geo-utils.js`)، يستورد منه
  كل من `opportunities-map.js` و`macro-context.js` باتجاه واحد فقط — تأكَّد الحل بالمنطق
  وبتجربة استيراد ديناميكي حقيقي (`import()`) للملفين الحقيقيين معاً في Node.js دون أي خطأ.
  لا تغيير في أي منطق قائم — نقل حرفي بالكامل لدوال المرحلة الخامسة إلى `geo-utils.js`.
  ⚠️ إحداثيات المشاريع تقريبية بالضرورة (مواقع عامة لمشاريع شاسعة المساحة)، وتُستخدَم كسياق
  جغرافي/تسويقي فقط — لا تدخل في أي حساب مالي فعلي.

## ٤. تصنيف الاستدامة (`sustainability-mostadam.js`، جديد) — مستدام/LEED/ESG

حقل جذر جديد كلياً `sustainability` (`{certificationTarget, currentRating, notes}`) عبر
`registerOpportunitySchemaExtender` — **لا يُغيِّر أي حساب فعلي**؛ أثر الشهادة (علاوة إيجار/
وفورات تشغيل) يُترَك للمحلل ليعكسه يدوياً بنفسه في خطوة "الإيراد والخروج" إن رغب، بالاستناد
لأرقام مرجعية عامة لا تقديراً آلياً خاصاً بفرصة لم تُصنَّف فعلياً بعد.

- حقل اختيار هدف/حالة التصنيف (خطوة معالج ٢ "عوامل الموقع الفيزيائية") — بنية "مستدام" السعودي
  الرباعية (أساسي/فضي/ذهبي/بلاتيني) + LEED كبديل مُعتمَد دولياً + "أخرى".
- **٤ فوائد اقتصادية مرجعية** (`ESG_BENEFITS`) — علاوة إيجار ٥-١٥٪ (نفس رقم `SECTOR_KNOWLEDGE.
  office` في `space-efficiency-library.js` بالضبط — إثراء متسق عبر الملفات لا تكرار متعارض)،
  وفورات تشغيل ١٠-٣٠٪، متطلبات مستثمرين مؤسسيين (ESG Mandate)، وأهلية تمويل أخضر.
  قسم تفاصيل يُعرَض لفرص الدخل دوماً (علاوة الإيجار ذات صلة دائماً) أو لأي فرصة حدَّدت هدف
  تصنيف فعلي، بلا مجموعة Firestore ولا صلاحيات CRUD (بعمد — دليل مرجعي ثابت بالكامل، لا سجلاً
  يُدار).

## ٥. Firestore

قاعدة جديدة واحدة فقط على مجموعة `saudiRegOverrides` (نفس نمط `fundFeeOverrides` بالضبط).
`rent-roll.js`، `macro-context.js`، و`sustainability-mostadam.js` لا تحتاج أي قاعدة جديدة —
بياناتها مُخزَّنة بالكامل داخل وثيقة الفرصة نفسها (حقول جذر جديدة) أو مرجعية ثابتة بالكود بلا
تخزين في Firestore أصلاً، فتُحمى بقاعدة `opportunities` الموجودة أصلاً دون أي تعديل عليها.

## ٦. التحقق من البناء

كل الملفات الأربعة الجديدة (`saudi-regulatory-library.js`، `rent-roll.js`، `macro-context.js`،
`sustainability-mostadam.js`) + `geo-utils.js` الجديد + المُعدَّلات (`opportunities-map.js`،
`main.js`، `firestore.rules`) فُحصت بـ`node --check` (صفر أخطاء صياغة) وباستيراد ديناميكي
حقيقي في Node.js لتأكيد سلامة شجرة الاستيراد الكاملة بلا أي دائرية. أربعة اختبارات وظيفية
مخصَّصة جديدة (`test_rentroll.mjs`، `test_macrocontext.mjs`، `test_saudireg.mjs`،
`test_sustainability.mjs`) تغطّي كل الدوال النقية بحتمية تامة (WALE/تركّز المستأجرين/جدول
مخاطر التجديد/Haversine) وكل الـhooks ومعالجات الإجراءات (بما فيها بوابة الصلاحيات الفعلية —
`canManageLibraries`/`canEditOpp` — على مستوى المعالج نفسه لا الواجهة فقط)، بالإضافة لتحديث
`test_oppmap.mjs` (تغطية زر تفعيل طبقة مشاريع رؤية ٢٠٣٠ الجديد). أُعيد تشغيل كل اختبارات
المراحل ٤+٥ القائمة (لم ينكسر شيء) + اختبار تكاملي مُوسَّع لكل الوحدات الاثنتي عشرة (المراحل
٤+٥+٦) معاً بنفس ترتيب `main.js` الحقيقي — بلا تعارض في مفاتيح `registerMainView`، أسماء
الإجراءات، أو المفاتيح الجذرية المُضافة عبر `registerOpportunitySchemaExtender` (`geo`،
`rentRoll`، `sustainability`، ...) — كل الاختبارات نجحت (`ALL PASSED`). لم يُتحقَّق عبر متصفح
حقيقي (Playwright) لنفس قيد البيئة المذكور في كل المراحل السابقة.

# المرحلة السابعة (٩ سبتمبر ٢٠٢٦) — الحوكمة والضبط المؤسسي (P0)

بعد المرحلة السادسة، طرح المستخدم مراجعة حوكمية مفصَّلة بسبع نقاط حرجة (P0)، وحسم الاتجاه
صريحاً: **"لا أريد الآن أن نصل إلى ٩.٥ بإضافة Features. أريد أن نصل إلى ٩.٥ بالحوكمة."** — أي
لا أنظمة/مكتبات جديدة هذه المرحلة، بل إصلاح فجوات حوكمية حقيقية في الأنظمة الموجودة أصلاً، مع
تسليم واحد فقط في النهاية يغطي البنود السبعة معاً (حسب طلب المستخدم الصريح المُكرَّر في كل
المراحل السابقة). القرارات المُلزِمة المتَّخَذة عبر AskUserQuestion: (أ) بناء كود Cloud
Functions كاملاً الآن ليُنشِره المستخدم بنفسه (لا قدرة نشر مباشر على مشروع Firebase الحقيقي من
هذه البيئة)، (ب) حوكمة المصادر: بناء آلية عامة قابلة لإعادة الاستخدام الآن، مُطبَّقة ابتدائياً
على الحقول الأشد أثراً (لا نطاق أضيق واحد فقط)، (ج) تسليم واحد للبنود السبعة معاً.

## ١. إصلاح صلاحيات اعتماد اللجنة (P0 #1) — `ic-workflow.js`

**الفجوة المكتشفة:** نموذج تسجيل قرار اللجنة كان يظهر ويُنفَّذ لأي شخص `core.canEditOpp(rec)`
صحيح معه — أي مالك/محرِّر الفرصة نفسها — بمعنى أن المحلل الذي أنشأ الفرصة يستطيع "اعتماد" صفقته
هو نفسه. مصفوفة الصلاحيات المطلوبة كانت: محلل → إنشاء/تعديل فرصه/رفع للجنة ✅، اعتماد ❌ — عضو
لجنة أول/مدير صندوق → اعتماد ✅.

**الإصلاح:** عرض/تنفيذ اتخاذ القرار مُقيَّد الآن بـ`canApproveIC(core)` (من `roles-permissions.js`
الموجود أصلاً من المرحلة الرابعة، عضو لجنة أول فأعلى) — دور مستقل تماماً عن ownership الفرصة —
بدل `core.canEditOpp(rec)`. الإصلاح طُبِّق في **طبقتين معاً** (لا تُغني إحداهما عن الأخرى): هنا
في الواجهة/المعالِج، وفي `firestore.rules` (`icOnlyChange` — راجع البند ٣ أدناه) حتى تكون
الحوكمة فعلية لا شكلية (محلل مموِّه للواجهة لا يمكنه تجاوز القيد الحقيقي في القاعدة). تحديث حالة
"الشرط" (`ic-toggle-condition`) بقي عمداً على `canEditOpp`: تتبُّع تنفيذي (checklist) لا قرار
لجنة، ومن الطبيعي أن يُحدِّثه صاحب الفرصة نفسه بعد استيفاء شرط اعتماد مشروط.

## ٢. بوابة قرار لجنة الاستثمار الحقيقية (P0 #2) — `ic-decision-gate.js` (جديد) + `ic-workflow.js`

**الفجوة المكتشفة:** "تسجيل قرار" كان نموذجاً حراً بلا أي شرط جهوزية فعلي — يمكن اعتماد صفقة
بعائد أقل من الحد الأدنى، أو ببند عناية واجبة حرج معلَّق، أو بلا مصدر موثَّق لأرقامها الحرجة.

**الإصلاح:** أربع بوابات حقيقية مُركَّبة في دالة نقية واحدة (`icReadiness(core, d, c)`،
`ic-decision-gate.js`) من دوال محسوبة سلفاً في ملفات موجودة أصلاً (بلا أي حقل Schema جديد أو
حساب مالي جديد):
- **البوابة المالية**: `equityIRR ≥ criteria.irrMin`، `MOIC ≥ criteria.moicMin`،
  `dscrMin ≥ criteria.dscrMin` (من `core.compute()` مباشرة).
- **بوابة العناية الواجبة**: صفر بنود حرجة معلَّقة (`ddStats` من `due-diligence.js`).
- **بوابة الحوكمة**: جودة بيانات ≥٨٥٪ + تغطية أدلة ≥٨٠٪ (`dataQualityStats` من `data-quality.js`
  + `evidenceCoverageStats` الجديدة من `evidence-tracking.js`، البند ٦ أدناه).
- **بوابة التسعير**: السعر الحالي ≤ الحد الأقصى للاستحواذ (`maxAcquisitionPrice` من المرحلة
  الثانية).

قسم تفصيلي جديد (`registerICDecisionGate`) يعرض "✅ IC READY" أو "🔴 NOT READY" مع أسباب كل بوابة
فاشلة. الأهم: **`ic-workflow.js` يستخدم `icReadiness` فعلياً لحظر الحفظ** — أي قرار
اعتماد/اعتماد بشروط على صفقة غير جاهزة يُرفَض بالكامل بلا أي أثر جانبي، إلا بتفعيل صريح لـ
**"تجاوز واعٍ"** (checkbox) **مع تبرير مكتوب** في حقل الأسباب معاً — والقرار المحفوظ يُسجِّل
`overridden:true` + `gateReasonsAtDecision` (أسباب عدم الجهوزية وقت القرار بالضبط) بشكل دائم،
حتى يبقى أثر واضح لأي اعتماد استثنائي لا يُخفى لاحقاً. قرارات غير الاعتماد (مراجعة/تعليق/رفض) لا
تحتاج هذه البوابة إطلاقاً — الحظر خاص فقط بفعل "الاعتماد".

## ٣. تحويل سجل التعديلات إلى سجل غير قابل للتلاعب (P0 #3) — `audit-trail.js` + `functions/`

**الفجوة المكتشفة:** `firestore.rules` كانت تسمح بـ`allow read, create: if isAuthorized();` على
مجموعة `oppAuditLog` — أي أن أي عميل مصرَّح له (بما فيه عميل مُعدَّل أو خبيث) يستطيع كتابة "سجل
تدقيق" مزيَّف بنفسه مباشرة، وهذا يُبطل قيمة السجل كدليل موثوق كلياً — سجل تدقيق يستطيع الفاعل
نفسه كتابته ليس سجل تدقيق حقيقياً.

**الإصلاح (النمط المطلوب صريحاً: "User → Application → Server Function → Immutable Audit
Event" بدل "User → Application → Firestore مباشرة"):**
- `firestore.rules`: `allow write: if false;` على `oppAuditLog` — **لا يمكن لأي عميل، ولا حتى
  الأدمن، الكتابة على هذه المجموعة نهائياً** بعد الآن (القراءة تبقى متاحة لعرض السجل).
- **`functions/index.js` (جديد بالكامل، Cloud Function v2)**: `mirrorOpportunityAuditLog`
  (`onDocumentWritten` على `opportunities/{oppId}`) تعمل بصلاحيات Admin SDK التي تتجاوز
  Security Rules بتصميم Firebase نفسه — هي الكاتب الوحيد الآن على `oppAuditLog`. تحسب الفرق
  العميق بين النسخة القديمة والجديدة من الفرصة (منطق `deepDiff`/`fieldLabel`/`IGNORE_PATHS`
  منقول، لا مستورَداً، من `audit-trail.js` — بيئة Cloud Functions منفصلة تماماً؛ **يجب تحديث
  الملفَين معاً** عند أي تعديل مستقبلي على قواعد الفرق، وهذا موثَّق بتعليق صريح في كل من الملفَين).
- `audit-trail.js`: الكتابة المحلية القديمة تعمل الآن **فقط في وضع الديمو** (`core.DEMO_MODE`،
  لا Firestore حقيقياً هناك أصلاً) — في الوضع الحقيقي `registerBeforeOpportunitySave` يتوقف فوراً
  (`if(!core.DEMO_MODE) return;`) قبل أي محاولة كتابة كانت سترفَض دائماً بلا داعٍ. حقل
  `audit.changeReason` لم يعد يُصفَّر تلقائياً بعد كل حفظ (كان هذا ضرورياً فقط للكتابة المباشرة
  القديمة من العميل) — تعديل عرض فقط في خطوة المعالج (لا تُعاد تعبئة القيمة السابقة كأنها للتعديل
  الحالي) لا تعديل بيانات.
- **⚠️ حد معماري صريح يستحق الذكر بوضوح للمستخدم**: كود `functions/index.js` كامل ومفحوص
  (`node --check` + اختبار Node.js مخصَّص يحمّل الوحدة فعلياً ويتحقق من `deepDiff`/`fieldLabel`/
  `actorFromData`) لكنه **لم يُنشَر ولا يمكن نشره من هذه البيئة** — نشره الفعلي يتطلب: خطة
  Firebase Blaze (Cloud Functions v2 لا تعمل على خطة Spark المجانية)، `firebase login`،
  `firebase use <project>`، ثم `cd functions && npm install && firebase deploy --only functions`
  من جهاز المستخدم نفسه أو بيئة CI له. تفاصيل كاملة في `functions/README.md`.

## ٤. حماية دفتر الصندوق/المستثمرين حسب الدور (P0 #4) — `firestore.rules`

**الفجوة المكتشفة:** مجموعات `investors`/`funds`/`commitments`/`capitalCalls`/`distributions`/
`transactions` كانت جميعها `allow read, write: if isAuthorized();` — أي عضو فريق مصرَّح له
(حتى محلل) يستطيع نظرياً تعديل/حذف سجلات مالية للصندوق، بلا أي تمييز بحسب دور "مدير الصندوق"
المُفترَض حصرياً على هذه البيانات.

**الإصلاح:** الكتابة على هذه المجموعات الست مقصورة الآن على `isFundManagerOrAbove()` فعلياً
(نفس دالة `canManageLibraries` بمنطقها بالضبط، مُعاد كتابتها بلغة Firestore Rules) — القراءة
تبقى متاحة لأي عضو مصرَّح له (شفافية داخلية طبيعية).

**⚠️ حد معماري صريح آخر يستحق التوضيح للمستخدم بلا تجميل**: نماذج CRUD الخاصة بالمستثمرين/
الصناديق نفسها مبنية **بالكامل داخل `core.js`** (منطق داخلي قديم من قبل أي نقطة توسّع)، ونقطة
التوسّع الوحيدة المتاحة لملفات الميزات (`registerActionHandler`) **لا تُستدعى إطلاقاً** لأي إجراء
يعرفه `core.js` داخلياً بالفعل (قاعدة معمارية ثابتة من المرحلة الأولى: لا لمس لـ`core.js`) — فلا
توجد طريقة لإخفاء أزرار "إضافة/تعديل/حذف مستثمر" لمستخدم غير مخوَّل من الواجهة نفسها بلا خرق هذه
القاعدة الأساسية. **الحل المُطبَّق فعلياً وهو الحل الصحيح مؤسسياً**: الحماية الحقيقية غير القابلة
للتجاوز في طبقة `firestore.rules` — فأي محاولة كتابة من محلل غير مخوَّل تُرفَض من قاعدة البيانات
نفسها (مُتحقَّق فعلياً عبر محاكي Firestore، لا نظرياً)، وهذا **أقوى** من إخفاء زر في الواجهة لا
أضعف منه (زر مخفي لا يمنع طلب API مباشر؛ قاعدة Firestore ترفضه دائماً) — تماماً ما أوصى به
المستخدم نفسه: *"أنصح بشدة بمراجعة قواعد Firestore على مستوى كل Collection واختبارها ضد مستخدم
Analyst/Senior IC/Fund Manager/Admin كلٌ على حدة، بدل الاعتماد على إخفاء الأزرار في الواجهة."*
الأثر الوحيد المتبقي على تجربة الاستخدام: محلل غير مخوَّل سيرى الأزرار لكن سيُفاجَأ برفض الحفظ من
الخادم (`accessDenied`/تنبيه خطأ موجود أصلاً في `core.js` لكل حالة رفض صلاحية) — تجربة أقل
سلاسة من إخفاء الزر مسبقاً، لكنها **صحيحة وآمنة حوكمياً**، وهو الترتيب الصحيح للأولويات عند
التعارض بين الاثنين.

## ٥. توحيد صلاحيات المقارنات (P0 #5) — `comparables.js`

**الفجوة المكتشفة:** "فجوة بين الواجهة والحماية الفعلية" — `firestore.rules` كانت (ولا تزال)
تُقيِّد الإضافة/التعديل/الحذف على `comparables` بـ`isFundManagerOrAbove()` بشكل صحيح تماماً منذ
المرحلة الرابعة، لكن `comparables.js` نفسه لم يكن يستدعي `canManageLibraries()` في أي مكان — فكان
نموذج الإضافة وزر الحذف ظاهرين وقابلين للتنفيذ لأي مستخدم في وضع الديمو/المحلي (حيث لا Firestore
حقيقي يردّ "صلاحية مرفوضة")، بخلاف كل مكتبة مرجعية أخرى مبنية بعد المرحلة الرابعة
(`benchmark-engine.js`، `saudi-regulatory-library.js`، ...) التي تستدعي `canManageLibraries()`
في الواجهة *و* في معالِج الإجراء نفسه.

**الإصلاح:** مطابقة تامة لهذا النمط الموحَّد — نفس دالة الفحص، في المكانين معاً (الواجهة
لإخفاء الأزرار، ومعالِج الإجراء نفسه لرفض التنفيذ حتى لو استُدعي الإجراء مباشرة متجاوزاً الواجهة
المخفية) — "نموذج صلاحيات واحد" بدل منطقَين منفصلَين قد يتباعدان مستقبلاً.

## ٦. حوكمة المصادر ومستويات الأدلة (P0 #6) — `evidence-tracking.js`

توسيع نظام تتبُّع الأدلة الموجود أصلاً من المرحلة الثانية (لا نظام مستقل جديد بالكامل، حسب قرار
المستخدم ببناء آلية عامة قابلة لإعادة الاستخدام مطبَّقة ابتدائياً على الحقول الأشد أثراً):

- **تصنيف مصدر رباعي المستوى** (`SOURCE_TIERS`): Tier 1 مصدر أساسي (عقد/سند رسمي) → Tier 2
  مؤسسي (تقرير CBRE/JLL/بنك) → Tier 3 سوقي (بوابة عقارية/تقدير سمسار) → Tier 4 تقدير المحلل —
  يُختار لكل حقل مصدره الآن، بجانب الثقة والتاريخ الموجودَين أصلاً.
- **علامة "حقل حرج" جديدة** (`critical:true/false`) على كل حقل رئيسي من `KEY_FIELDS`: سعر
  الأرض، الإيجار، الإشغال، القيمة السوقية (WACC)، سعر البيع، تكلفة البناء = حرجة؛ FAR، السايبور،
  الهامش، معدل الرسملة عند الخروج، نمو قيمة الأرض = غير حرجة.
- **دالة نقية جديدة `evidenceCoverageStats(core, d)`** (يستخدمها `ic-decision-gate.js` مباشرة
  في بوابة الحوكمة أعلاه): نسبة التغطية الكلية، نسبة التحقق، وقائمتان مفصولتان — حقول حرجة بلا
  مصدر (**🔴 عائق للجنة / IC BLOCKER**) وحقول غير حرجة بلا مصدر (**⚠️ غير موثَّق / UNSOURCED**).
- **تحقّق (Verification) مستقل عن الإدخال**: من كتب المصدر ليس بالضرورة من يتحقق منه —
  `verifiedBy`/`verifiedAt` جديدان لكل حقل، بزر "✅ تحقّق" مقيَّد بـ`canApproveIC` (عضو لجنة أول
  فأعلى، نفس دالة اعتماد اللجنة — لا حاجة لدور جديد مستقل لمجرد التحقق من مصدر). **تغيير نص
  المصدر يُبطل التحقق السابق تلقائياً** — تعديل الرقم أو مصدره لاحقاً لا يُبقي على شارة "مُتحقَّق"
  قديمة قد تكون غير صحيحة بعد التعديل.

## ٧. التسعير الموثَّق بالإصدارات (P0 #7) — `underwriting-versions.js` (جديد)

**السؤال الذي لا جواب له بلا هذا النظام: "هل تغيّرت الصفقة أم تغيّر افتراضنا؟"** — بلا لقطات
مؤرَّخة للأرقام الرئيسية عند لحظات حاسمة، يستحيل التمييز بين تغيّر حقيقي في الصفقة وتغيّر في
افتراض المحلل نفسه بمرور الوقت.

- **مجموعة Firestore مستقلة ومسطَّحة جديدة** (`underwritingVersions`، بحقل `oppId` — نفس نمط
  `oppAuditLog` بالضبط لا subcollection)، **append-only بتصميم `firestore.rules`**: أي عضو
  مصرَّح له يقدر يُنشئ نسخة جديدة، لكن التعديل/الحذف مقصور على الأدمن فقط — "تعديل السجل
  التاريخي نادر واستثنائي، لا إجراء اعتيادي".
- **لقطة تلقائية عند كل اعتماد لجنة جديد** (`registerBeforeOpportunitySave`، بلا أي تعديل على
  `ic-workflow.js` نفسه — الاكتشاف يتم بمقارنة عدد `ic.decisions` قبل/بعد الحفظ فقط): أي قرار
  `approve`/`approve_conditions` جديد يُسجِّل تلقائياً لقطة `v4_ic_approved` بالأرقام الحقيقية
  (سعر الأرض، Equity IRR، MOIC، DSCR) **كأرقام ثابتة وقت الحفظ لا كصيغة تُعاد حسابها لاحقاً** —
  لأن الغاية بالتحديد هي كشف تغيّر الافتراضات نفسها بمرور الزمن؛ إعادة حساب النسخ القديمة
  بافتراضات اليوم يُبطل الغاية كلياً. قرارات غير الاعتماد (مراجعة/تعليق/رفض) لا تُنشئ لقطة —
  تبقى ظاهرة في سجل قرارات اللجنة نفسه دون تكرار.
- **لقطة يدوية** في أي وقت (بعد نتيجة عناية واجبة، أو عرض بائع مضاد، ...) بزر مباشر، بصلاحية
  `canEditOpp` (صاحب/محرِّر الفرصة، لا حصراً عضو لجنة — التوثيق هنا تسجيل وقائع لا قرار اعتماد).
- **حقل جذر جديد `actuals`** (الأداء الفعلي بعد الإغلاق/التنفيذ: السعر الفعلي، Equity IRR
  الفعلي، MOIC الفعلي، تاريخ، ملاحظات) — يُمكِّن مقارنة "الفعلي مقابل المُسعَّر" مباشرة على شاشة
  الفرصة (فرق بالنقاط عن آخر تسعير حالي)، وهو الأساس لاحقاً لتعلّم المعايير المرجعية من صفقات
  مُغلَقة فعلياً (خارج نطاق هذه المرحلة — فكرة إثراء مستقبلية).
- جدول زمني كامل على صفحة كل فرصة: كل نسخة محفوظة + صف "الحالي (حيّ)" يُعاد حسابه لحظياً للمقارنة
  المباشرة.

## ٨. Firestore — ملخص التغييرات (البنود ١، ٣، ٤، ٥، ٧ أعلاه معاً)

- `icOnlyChange`: `senior_ic` فأعلى يقدر الآن يسجّل قرار لجنة على فرصة *لا يملكها*، لكن **فقط**
  حقل `ic` (لا أي حقل مالي آخر "مُخبَّأ" في نفس الطلب — `MapDiff.affectedKeys().hasOnly(['ic'])`)
  — محلل عادي بلا دور لجنة لا يقدر على هذا المسار إطلاقاً حتى لو كان يملك الفرصة.
- `oppAuditLog`: `allow write: if false;` — بلا استثناء لأي دور، حتى الأدمن (الكتابة الحقيقية
  من Cloud Function بصلاحيات Admin SDK فقط).
- `investors`/`funds`/`commitments`/`capitalCalls`/`distributions`/`transactions`: الكتابة
  مقصورة على `isFundManagerOrAbove()`، القراءة متاحة لأي عضو مصرَّح له.
- `comparables`: الكتابة مقصورة على `isFundManagerOrAbove()` (كانت هذه القاعدة صحيحة أصلاً منذ
  المرحلة الرابعة — الفجوة كانت في الواجهة فقط، أُصلحت في البند ٥ أعلاه).
- `underwritingVersions`: إنشاء لأي عضو مصرَّح له، تعديل/حذف للأدمن فقط.

## ٩. التحقق من البناء — محاكي Firestore حقيقي + اختبارات Node.js وظيفية

**تحديداً استجابةً لتوصية المستخدم الصريحة** بعدم الاعتماد على إخفاء الأزرار في الواجهة، بُني
اختبار **حقيقي** (لا محاكاة يدوية) لكل قواعد `firestore.rules` عبر **Firebase Local Emulator
Suite** (`@firebase/rules-unit-testing`، `tests/rules/`) — تشغيل فعلي لمحرك تقييم قواعد Firebase
نفسه، لا افتراض نظري عن سلوك القواعد. **٣٢ اختباراً نُفِّذت فعلياً ضد المحاكي الحقيقي، لكل من
البنود ١، ٣، ٤، ٥، ٧ أعلاه**، ولكل الأدوار الأربعة كلٌ على حدة (Analyst/Senior IC/Fund
Manager/Admin) — بريد غير مصرَّح له إطلاقاً لا يقدر يقرأ أي شيء، ونزاهة النِّسبة
(`attributionHonest`) مؤكَّدة (لا يقدر أي مستخدم يزيّف `meta.updatedBy`). آخر تشغيل:
`ALL PASSED (against a real Firestore emulator, not a mock)`، خروج بالرمز ٠ — تفاصيل التشغيل
والقيد التقني المكتشَف حول مسار ملف القواعد في `firebase-tools` نفسه (`tests/rules/README.md`).

بالإضافة لذلك، ستة اختبارات Node.js وظيفية مخصَّصة جديدة (بنفس منهجية كل المراحل السابقة —
محاكاة `core` بكل نقاط التوسّع المستخدَمة، بلا متصفح): `test_icworkflow.mjs` (صلاحية الاعتماد +
بوابة الجهوزية + التجاوز الواعي بتبرير)، `test_icgate.mjs` (البوابات الأربع منفردة ومجتمعة)،
`test_evidence.mjs` (مستوى المصدر + التحقق + إبطاله التلقائي عند تغيّر النص)، `test_comparables.mjs`
(الفجوة بين الواجهة والمعالِج، للتأكيد المزدوج)، `test_underwriting.mjs` (اللقطة التلقائية/
اليدوية + حقل الفعلي)، و`test_functions.mjs` (تحميل وحدة `functions/index.js` فعلياً والتحقق من
منطق `deepDiff`/`fieldLabel`/`actorFromData` بلا اعتماد Firebase حقيقي). أُعيد تشغيل اختبار
تكاملي مُوسَّع (`test_integration_phase7.mjs`) يسجّل كل الوحدات الثماني عشرة (المراحل ٤+٥+٦+٧)
معاً بنفس ترتيب `main.js` الحقيقي — بلا تعارض في مفاتيح `registerMainView`، أسماء الإجراءات، أو
المفاتيح الجذرية الجديدة (`ic`، `evidence`، `actuals`) — كل الاختبارات نجحت (`ALL PASSED`).

**لم يُتحقَّق عبر متصفح حقيقي (Playwright)** لنفس قيد البيئة المذكور في كل المراحل السابقة، وبلا
أي محاولة نشر فعلية لـ`functions/index.js` (يتطلب بيانات اعتماد Firebase حقيقية وخطة Blaze —
غير متاحة من هذه البيئة، والنشر مسؤولية المستخدم نفسه كما تقرَّر عبر AskUserQuestion في بداية هذه
المرحلة).
