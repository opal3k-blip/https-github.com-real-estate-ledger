# مطابقة "التقييم المؤسسي المستقل" (١٣ سبتمبر ٢٠٢٦) مع الكود الفعلي + ما تم تنفيذه اليوم

تاريخ هذا المستند: ١٤ سبتمبر ٢٠٢٦. هذا مستند مستقل عن `مراجعة_النسخة_رقم_1.md` (المراجعة الأولى) — هذا المستند مخصص حصراً لمطابقة الوثيقة الثانية التي أرسلتها ("OPAL — التقييم المؤسسي المستقل"، نتيجة ٧٢/١٠٠) بندًا ببند مع الكود الحقيقي، ثم تنفيذ ما ثبت أنه فجوة حقيقية.

## الخلاصة المباشرة أولاً

أغلب بنود P0 في الوثيقة الثانية **موجودة فعلاً كاختبارات انحدار مسجَّلة في `tests/financial/validation-suite.cjs` بنفس الصياغة تقريباً، وكلها PASS ضد الكود الحالي**:

```
017  Follow-on negative equity cash flows are included in PIC and MOIC denominator   → PASS
018  IC gate enforces DD completion, evidence verification, and planning feasibility → PASS
019  In-kind commitments must be asset-bound before cash timing deducts them          → PASS
020  Waived capital calls are excluded from active called capital                     → PASS
021  Portfolio NAV and Net IRR labels are explicitly indicative until independent
     valuations exist                                                                 → PASS
022  Cloud Functions expose server-side business invariant commands                   → PASS
```

هذا التطابق شبه الحرفي بين عناوين اختبارات موجودة سلفاً في الكود وبين بنود "P0" في الوثيقة التي أرسلتها يعني على الأرجح أحد شيئين: (أ) هذه الوثيقة الثانية تعكس نسخة **أقدم** من النظام سبق إصلاحها في مرحلة لاحقة موثَّقة (المرحلتان ٦ و٧)، أو (ب) هي تقرير جرى تحليله دون تشغيل فعلي للكود المرفق. في الحالتين: **لا يوجد سبب لإعادة "إصلاح" منطق مالي يعمل بشكل صحيح فعلاً ومختبَر حياً بمحرك حساب حقيقي (لا محاكاة)** — إعادة كتابته الآن بلا داعٍ يخاطر بكسر شيء يعمل.

النقطتان الحقيقيتان اللتان أكَّدتهما الوثيقة الثانية ولم تكونا موثَّقتين بنفس الوضوح، تم إصلاحهما اليوم فعلياً (تفاصيل تحت). كل تعديل: (١) لم يلمس `core.js` إطلاقاً (التزاماً بقاعدة المشروع الثابتة)، (٢) اجتاز فحص الصيغة (`check-js.mjs`، ٥٨ ملفاً)، (٣) لم يكسر أياً من اختبارات `validation-suite.cjs` الـ٢٣ (لا تزال كلها PASS)، (٤) تم رفعه فعلياً لمجلد مشروعك الحقيقي على جهازك (`C:\Users\DELL\Dropbox\PC\Downloads\reop-github`) عبر الاتصال المباشر بجهازك — ليس مجرد ملف أرسلته لك لتنسخه يدوياً.

---

## ١. الجدول الكامل: كل بند P0 في وثيقتك الثانية مقابل الكود الفعلي

| # | ادعاء الوثيقة الثانية | الحالة الفعلية في الكود | الحكم |
|---|---|---|---|
| P0-1 | MOIC/PIC لا تحتسب نداءات حقوق ملكية لاحقة (follow-on) بعد السنة صفر | `core.js:1620`: `contributedEquity = equityCF.reduce((sum, cf)=> sum + (cf<0 ? Math.abs(cf) : 0), 0)` — يجمع **كل** التدفقات السالبة عبر كل السنوات، ليس فقط سنة صفر. اختبار `017` PASS يثبت هذا حياً. | ❌ **مردود** — غير صحيح على الكود الحالي |
| P0-3 | لا توجد بوابة جدوى تخطيطية (Planning/Zoning Feasibility Gate) | `ic-decision-gate.js`: دالة `planningFeasibility()` كاملة تحسب `allowedFloors`/`setback`/`allowedFootprint`/`requiredFloors` وتُدرَج كبوابة خامسة إلزامية ضمن `icReadiness()`. اختبار `018` PASS. | ❌ **مردود** |
| P0-4 (جزء) | بوابة اللجنة تتحقق فقط من `criticalPending===0` بلا حد أدنى لنسبة إنجاز العناية الواجبة، وتقبل أدلة Tier-4/غير موثَّقة | `icReadiness()`: `ddOk = criticalPending===0 && pct>=DD_COMPLETION_MIN(0.80)` — شرطان معاً لا شرط واحد. `governanceOk` يتحقق أيضاً من جودة البيانات ≥٠.٨٥، تغطية الأدلة ≥٠.٨٠، الأدلة المُتحقَّق منها ≥٠.٦٠، وضعف/تقادم الأدلة الحرجة (>٧٣٠ يوماً). اختبار `018` PASS. | ❌ **مردود** |
| — | مساهمات عينية تُحتسَب مرتين عبر أكثر من أصل في نفس الصندوق | `cash-flow-timing.js:137`: `if(cm.data.inKindAssetId && cm.data.inKindAssetId===oppId) totalInKind += amt;` — الاحتساب مقيَّد صراحة بالأصل المرتبط فعلياً بمعرّفه، لا يمكن احتسابه لأصل آخر. اختبار `019` PASS. | ❌ **مردود** |
| — | نداءات رأس المال لا تُقارَن بسقف التزام المستثمر (لا حارس سعة) | حارسان فعليان مستقلان: (أ) `core.js:775-796 validateIfDraft()` يمنع تعليم نداء "مسدَّد" لو تجاوز `committed`؛ (ب) `capital-allocation-engine.js` (`deployableCashForFund`) يمنع ربط أصل بصندوق لو تجاوز التخصيص المستهدف رأس المال الفعلي المتاح (`paidIn - distPaid - allocatedElsewhere`). كلاهما عميل (client-side) — انظر البند الحقيقي أدناه. | ⚠️ **موجود جزئياً، والفجوة الحقيقية موثَّقة سلفاً** |
| — | NAV محسوبة من MOIC نهائي افتراضي وقت الاكتتاب، لا تقييم فعلي مستقل | صحيح جزئياً — لكن `core.js` نفسه يُعلِّم هذا بوضوح: تعليق `fundEquityAndValue()` "تقدير تقريبي… **وليس تقييماً مستقلاً معتمَداً (NAV)**". اختبار `021` يتحقق تحديداً من أن التسميات في الواجهة صريحة "indicative until independent valuations exist". | ⚠️ **صحيح كفجوة منهجية، لكنه مُفصَح عنه بوضوح للمستخدم وليس خطأً مخفياً** |
| — | لا يوجد دفتر نقدية/رأس مال قابل للنشر فعلياً على مستوى الصندوق (Deployable Capital Ledger) | `fundLedgerSummary()` في `core.js` يحسب فعلاً `deployableCash = paidIn - distPaid`، ويُستخدَم في `capital-allocation-engine.js`. الفجوة الحقيقية (موثَّقة سلفاً في `docs/SECURITY_RULES_REVIEW.md` توصية ٥): هذا الحساب عميل بالكامل اليوم، والدوال الخلفية (`postCapitalCall`/`linkAssetToFund`) التي تُعيد نفس التحقق داخل معاملة Firestore حقيقية **موجودة وجاهزة لكن غير موصولة بالواجهة** ولا منشورة (تحتاج خطة Blaze). | ✅ **فجوة حقيقية، معروفة سلفاً، غير مُصلَحة بعد** |
| — | محرك المخاطر يمنح فئات لم تُقيَّم إطلاقاً تصنيف "منخفضة" ضمنياً | `risk-engine.js` (قبل اليوم): `probability:1, impact:1` افتراضياً → `bandOf(1)` = "منخفضة". هذا صحيح فعلاً ولم يكن هناك أي تمييز بين "قُيِّمت فعلاً بدرجة منخفضة" و"لم يفتحها أحد". | ✅ **فجوة حقيقية — تم إصلاحها اليوم (تفاصيل §٢)** |
| — | الحد الأقصى لسعر الاستحواذ يعتمد على IRR فقط، لا القيد الأكثر تقييداً بين عدة قيود (DSCR مثلاً) | `max-acquisition-price.js`: البحث الثنائي يستهدف `equityIRR >= targetIRR` فقط. لا فحص لـ`dscrMin` عند السعر الناتج — قد يكون السعر "الأقصى المسموح" يخالف حد DSCR المعتمد (١.٣٠×) رغم تحقيقه IRR. | ✅ **فجوة حقيقية — لم تُصلَح بعد (انظر §٣ للتوصية)** |

---

## ٢. ما تم إصلاحه فعلياً اليوم

### أ) محرك المخاطر (`src/features/risk-engine.js`)
المشكلة الحقيقية: فئة مخاطر لم يفتحها أحد (لا تزال بالقيمة المصنعية احتمالية=أثر=١) كانت تظهر في تقرير اللجنة كـ"مخاطر منخفضة" — راحة زائفة قد تُضلِّل قرار استثماري حقيقي.

الإصلاح (بلا أي لمسة لـ`core.js`، وبلا أي حاجة لترحيل بيانات قديمة — القيمة مُشتقة لا مخزَّنة):
- فئة تُعتبَر "مُقيَّمة فعلاً" فقط لو خرجت عن الافتراضي المصنعي (احتمالية/أثر مختلفان عن ١) أو أُدخِل لها إجراء تخفيف/مسؤول/موعد نهائي.
- الفئات غير المُقيَّمة تظهر الآن بعلامة منفصلة "لم تُقيَّم" (رمادية) بدل "منخفضة" (خضراء)، ولا تُحتسَب ضمن "المخاطر الإجمالية".
- إضافة عداد بارز: "⚠️ فئات لم تُقيَّم بعد: س/١١" يظهر في أعلى القسم متى ما كان هناك أي فئة مفتوحة.

### ب) الدرجة الاستثمارية المركّبة (`src/features/investment-score.js`)
اكتشفتُ أثناء التحقق أن نفس المشكلة كانت تتكرر بأثر أكبر: محور "المخاطر" (وزنه ١٠٪ من الدرجة الكلية ٠-١٠٠) كان يمنح **١٠٠/١٠٠** (أفضل درجة ممكنة) لأي فرصة سجل مخاطرها فارغ بالكامل — لأن أعلى درجة بين ١١ فئة كلها "١×١=١" هي ١، والمعادلة تحوّل ١ إلى ١٠٠٪. أي فرصة لم تخضع لأي تحليل مخاطر كانت تُصنَّف على أنها الأقل خطورة *استثمارياً* في الترتيب المركَّب.

الإصلاح: سجل مخاطر لم يُفتَح إطلاقاً يمنح الآن **صفر/١٠٠** على هذا المحور (بدل ١٠٠)، وسجل مُقيَّم جزئياً يُخصَم من درجته بنسبة الفئات غير المُقيَّمة. هذا يمنع فرصة "لم تُدرَس مخاطرها" من الظهور بمظهر أفضل من فرصة قُيِّمت فعلاً ووُجِدت متوسطة المخاطر.

### ج) `firebase.json` — نشر Storage Rules
`storage.rules` كان موجوداً ومكتوباً جيداً لكن غير مذكور في `firebase.json`، فلن يُنشَر أبداً عبر `firebase deploy` العادي (نشرت أشهراً وربما لا أحد لاحظ أن قواعد رفع الملفات ليست فعّالة على الخادم الحقيقي). أضفتُ:
```json
"storage": { "rules": "storage.rules" }
```

**الملفات الثلاثة المعدَّلة رُفعت فعلياً إلى مجلد مشروعك الحقيقي على جهازك** (`src/features/risk-engine.js`، `src/features/investment-score.js`، `firebase.json`) — لست بحاجة لنسخ أي شيء يدوياً لهذه الثلاثة. راجعها في Git لترى الفرق (`git diff`) قبل أي `commit`/`push` إن أردت.

---

## ٣. توصية غير مُنفَّذة بعد (تحتاج قرارك)

**الحد الأقصى لسعر الاستحواذ (`max-acquisition-price.js`) يتجاهل قيد DSCR.** الإصلاح المقترح: تعديل البحث الثنائي ليطلب تحقق **كل** من `equityIRR >= targetIRR` **و** `dscrMin >= criteria.dscrMin` معاً (القيد الأكثر تقييداً هو الحاكم). هذا يمس منطقاً مالياً حساساً (يُستخدَم أيضاً في `negotiation.js` لحساب Walk-away Price)، ولا يوجد له اختبار انحدار حالياً يغطي تقاطع DSCR/IRR تحديداً — أفضِّل أن أكتب اختباراً جديداً في `validation-suite.cjs` أولاً يثبت السلوك الصحيح قبل تعديل المنطق، بدل تعديل حساب مالي بلا شبكة أمان مماثلة لبقية المحرك. أخبرني إن أردت أن أبدأ بهذا الآن.

---

## ٤. نقطة قرار: الفجوة الحقيقية الأكبر المتبقية

الفجوة الحقيقية الوحيدة من الوثيقتين معاً (الأولى والثانية) التي لم تُحَل بعد وتمسّ فعلاً سلامة رأس المال هي: **الدوال الخلفية `approveOpportunity`/`linkAssetToFund`/`postCapitalCall` مكتوبة وجاهزة ومُختبَرة (اختبار `022` PASS) لكنها غير موصولة بالواجهة وغير منشورة على مشروع Firebase الحقيقي.** حتى تُنشَر وتُوصَل، كل الحراسات (منع تجاوز الالتزام، منع تخصيص رأس مال يفوق المتاح) تبقى من جانب العميل فقط — قابلة للتجاوز نظرياً من مستخدم يتعامل مع Firestore مباشرة (لا من الواجهة العادية).

نشر هذه الدوال يتطلب: (أ) ترقية مشروع Firebase إلى خطة Blaze (الدفع بالاستخدام — التكلفة الفعلية للاستخدام العادي لمنظومة بحجمكم تكون ضئيلة جداً عادة، لكنها تتطلب ربط بطاقة/حساب فوترة)، (ب) تشغيل `firebase deploy --only functions`، (ج) إضافة استدعاءات `httpsCallable` في الواجهة بدل الكتابة المباشرة الحالية لهذه العمليات الثلاث. هذا عمل حقيقي متوسط الحجم (ليس تعديل سطر) وله أثر تشغيلي (يغيّر تدفق الحفظ الفعلي لهذه العمليات) — **أحتاج موافقتك الصريحة على المضي فيه** قبل البدء، تحديداً لأنه يتطلب منك خطوة خارج الكود (تفعيل Blaze) لا أستطيع القيام بها نيابة عنك.

---

## الفهرس السريع لبقية طلباتك

- **كود `firestore.rules` الجاهز للصق المباشر**: في رسالتي في المحادثة (وليس هذا الملف) — طلبتَ نسخه مباشرة فهو موجود كـ code block منفصل ليسهل تحديده كاملاً والنسخ منه.
- **تعطيل/سحب رمز جيت هب المسرَّب**: خطوات مباشرة في رسالتي في المحادثة.
- **توضيح طلب "رفع ملفات جيت هب لأتفق معاها"**: سؤال توضيحي في رسالتي في المحادثة.
